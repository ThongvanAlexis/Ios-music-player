---
phase: 01-installable-native-player
plan: "10"
type: execute
wave: 10
depends_on: ["01-09"]
files_modified: ["Tests/Fixtures/manifest.json","Tests/Fixtures/fixture-toolchain.json","scripts/generate_fixtures.py","MusicPlayerTests/AppleCodecMatrixTests.swift","docs/codec-matrix.md","MusicPlayerTests/LongRecordingTests.swift"]
autonomous: true
requirements: ["PLAY-01","PLAY-03","APP-01"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every required matrix cell is measured on Apple decoding before app decoder dependencies are adopted; expected unsupported-codec results remain separate from harness failures."
    - "The matrix includes MP3 CBR/VBR, Ogg Vorbis, Ogg Opus, FLAC, PCM WAV, AAC M4A, ALAC M4A and AAC MP4 with video, plus invalid/truncated/metadata-free inputs."
    - "Measurements record open, bounded read, duration and beginning/middle/end seek outcomes, decoder, known landmarks, bytes/hash, channels/rate, OS and source SHA."
    - "iOS 17 runtime coverage is separate from the deployment-target build and current-OS simulator/device evidence."
  artifacts:
    - path: "Tests/Fixtures/manifest.json"
      provides: "Pinned legal fixture identity and expected landmarks"
    - path: "scripts/generate_fixtures.py"
      provides: "Bounded fixture creation with reviewed tool provenance"
    - path: "MusicPlayerTests/AppleCodecMatrixTests.swift"
      provides: "Apple-first runtime codec/seek matrix"
    - path: "docs/codec-matrix.md"
      provides: "Measured gaps and environment evidence"
  key_links:
    - from: "MusicPlayerTests/AppleCodecMatrixTests.swift"
      to: "MusicPlayer/Audio/ApplePCMDecoder.swift"
      via: "Exercise actual decoder with hashed fixture bytes"
    - from: "Tests/Fixtures/manifest.json"
      to: "scripts/generate_fixtures.py"
      via: "Shared matrix/landmark and fixture provenance definition"
    - from: "docs/codec-matrix.md"
      to: "NativeCodec/sources.lock.json"
      via: "Only measured unsupported cells authorize conditional app decoders"
---

# 01-10: Measure Apple decoding against the complete format matrix

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Establish the Apple runtime coverage and exact missing decode paths before choosing app dependencies.
Purpose: Resolve format support using measured file/codec behavior, not filename assumptions or source-only inspection.
Output: Tests/Fixtures/manifest.json, scripts/generate_fixtures.py, MusicPlayerTests/AppleCodecMatrixTests.swift, docs/codec-matrix.md.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW manifest.json is the single fixture matrix: id, codec, container, filename, sourceURL/revision, license, SHA256, channels, sampleRate, duration, landmarkPositions and expectedFailure. Generated multi-hour data lives under ignored build/fixtures, with compact legal fixtures checked in only at exact manifest-listed paths. NEW fixture-toolchain.json pins every external fixture-generation tool to canonical version/revision and archive/executable digest with license/build-review record. This is test tooling only and never an implicit production decoder dependency. New docs/codec-matrix.md has one row per required case/environment and distinguishes unsupported Apple capability, malformed media and failed harness.

## Dependencies and scope

Needs the preceding plan's implemented capability; creates the artifacts below. Sequential wave 10 follows shared-file ownership and the project's sequential execution setting.

<tasks>
<task type="auto" tdd="true">
  <name>01-10-T1: Create known audio fixtures and exercise the Apple path</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>Tests/Fixtures/manifest.json, Tests/Fixtures/fixture-toolchain.json, scripts/generate_fixtures.py, MusicPlayerTests/AppleCodecMatrixTests.swift, docs/codec-matrix.md</files>
  <behavior>The suite rejects missing/hash-wrong matrix entries, emits one non-skipped runtime result per case and checks known PCM landmarks at start/middle/end. Corruption and Apple unsupported results carry original cause, while missing executables or zero fixtures fail the harness.</behavior>
  <action>Create the complete matrix named above without adding app dependencies. Prefer legally redistributable official Apple/Xiph/FFmpeg test assets at immutable source revisions; record origin, redistribution terms, actual hash and independent expected format/duration/landmarks. Generate a project-owned PCM WAV tone/landmark source using Python standard library in bounded chunks and Apple afconvert for supported encodings. For compressed encodings unavailable in Apple tools, resolve one maintained official FFmpeg release and exact source/archive digest into fixture-toolchain.json, inspect its license, required encoder/transitive sources and build commands before executing any of them, then produce the missing test fixtures with an explicit encoder/muxer list. If fixture code cannot be safely reviewed, use reviewed immutable official test samples instead; do not invent a pin or execute repository setup scripts. Record all tools/commands/versions and preserve the same audio landmark source across generated encodings. Extend native test resource membership through the existing synchronized file groups and manifest-driven test loader. Run every codec first through ApplePCMDecoder/AVAudioFile using actual buffers and seeks. Expected unsupported results are recorded observations, not a passing requirement; harness or unexpected failures return nonzero. Keep source-only checks visibly separate from runtime results.</action>
  <acceptance_criteria>A nonempty runtime matrix covers all nine required codec/container rows with legal reproducible bytes, hashes and real Apple open/read/seek results; missing rows are failures.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite AppleCodecMatrixTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>A nonempty runtime matrix covers all nine required codec/container rows with legal reproducible bytes, hashes and real Apple open/read/seek results; missing rows are failures.</done>
</task>

<task type="auto" tdd="true">
  <name>01-10-T2: Measure long seeks and minimum-OS support before decoder selection</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayerTests/AppleCodecMatrixTests.swift, MusicPlayerTests/LongRecordingTests.swift, Tests/Fixtures/manifest.json, scripts/generate_fixtures.py, docs/codec-matrix.md</files>
  <behavior>A three-hour generated input and short equivalent keep scheduled frame limits constant; seek landmarks near start/middle/end meet recorded tolerances; iOS17/current environments are named distinctly and absent runtime cannot report success.</behavior>
  <action>Generate the three-hour fixture off the main/test UI thread in ignored build output; cover MP3 VBR/Opus/MP4 seek landmarks where Apple opens them, mixed sample rates and repeated forward/backward seeks. Record initial engineering acceptance thresholds in the manifest/config: confirmed seek error at most 100 ms for known tone landmarks, scheduled-buffer count never above configuration, and resident-memory plateau within 32 MiB above the same short-file warm baseline after sustained playback. These are targets to verify, not existing performance claims. Measure actual OS/toolchain/source and performance with available native instruments/process metrics; adapt only with evidence and record any failed target. Run the complete short matrix on iOS 17 and current supported runtime; if the hosted runner lacks iOS 17, obtain a compatible official runtime when available or use a physical supported older device. Missing minimum-OS runtime evidence remains outstanding and does not authorize raising deployment target. Add physical listening checks for codec transitions and matched audio landmarks. Produce the exact measured gap list consumed by Plans 11 and 12: container path gap versus required codec gap, with original error and affected environment.</action>
  <acceptance_criteria>Long-recording measurements have explicit numbers/results and the gap list identifies only failed required Apple cells. Minimum-OS runtime absence is visible and blocks claiming that environment verified.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite AppleCodecMatrixTests --suite LongRecordingTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
    <human-check>Play the compact required codec fixtures on the target iPhone; record audible start and seek landmarks. Minimum-OS hardware/runtime and memory checks remain pending if unavailable.</human-check>
  </verify>
  <done>Long-recording measurements have explicit numbers/results and the gap list identifies only failed required Apple cells. Minimum-OS runtime absence is visible and blocks claiming that environment verified.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

External fixture/source bytes → reviewed test tool → decoder; compressed media → bounded PCM.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-10-1 | Tampering | Fixture tool/source supply | high | mitigate | Canonical immutable source/version and digest/license/build review before execution; isolated test outputs. |
| T-01-10-2 | Denial of service | Malformed/long inputs | high | mitigate | Bound decoding and wall-clock work; retain causes and use sanitizer execution where supported. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Every required matrix cell is measured on Apple decoding before app decoder dependencies are adopted; expected unsupported-codec results remain separate from harness failures. The matrix includes MP3 CBR/VBR, Ogg Vorbis, Ogg Opus, FLAC, PCM WAV, AAC M4A, ALAC M4A and AAC MP4 with video, plus invalid/truncated/metadata-free inputs. Measurements record open, bounded read, duration and beginning/middle/end seek outcomes, decoder, known landmarks, bytes/hash, channels/rate, OS and source SHA. iOS 17 runtime coverage is separate from the deployment-target build and current-OS simulator/device evidence.
</success_criteria>

## Flagged assumptions

EDGE PLAY-01 and PLAY-03/unclassified remain unresolved classifier assumptions, tied to actual matrix/long-seek evidence. They are not auto-dismissed or converted into a backstop pass. Fixture-generation source pin resolution is implementation work; no external binary or source build is authorized merely by a README.

<output>
Create .planning/phases/01-installable-native-player/01-10-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
