---
phase: 01-installable-native-player
plan: "12"
type: execute
wave: 12
depends_on: ["01-11"]
files_modified: ["NativeCodec/DecoderBridge.c","NativeCodec/DecoderBridge.h","MusicPlayer/Audio/XiphPCMDecoder.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayerTests/CodecMatrixTests.swift","MusicPlayer/Audio/MP4PCMDecoder.swift","MusicPlayerTests/LongRecordingTests.swift","docs/codec-matrix.md"]
autonomous: true
requirements: ["PLAY-01","PLAY-03"]
estimate:
  tokens: 30667
  raw_tokens: 30667
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Required formats all decode and seek through the shared bounded producer; conditional code is adopted only for measured Apple gaps."
    - "Vorbis/Opus chained channel/rate changes and MP4 timestamps/preroll preserve correct source-time seeking and original errors."
    - "No whole-file transcoding or permanent PCM duplication is introduced."
  artifacts:
    - path: "MusicPlayer/Audio/XiphPCMDecoder.swift"
      provides: "Conditional bounded Xiph cursor/error conversion"
    - path: "MusicPlayer/Audio/MP4PCMDecoder.swift"
      provides: "Conditional timestamp-aware Apple MP4 audio reader"
    - path: "docs/codec-matrix.md"
      provides: "Final required-codec runtime results"
  key_links:
    - from: "NativeCodec/DecoderBridge.c"
      to: "MusicPlayer/Audio/XiphPCMDecoder.swift"
      via: "Bounded local-file decoding and original result codes"
    - from: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      to: "MusicPlayer/Audio/PCMDecoder.swift"
      via: "Select measured format path and feed one producer"
---

# 01-12: Measured Ogg and MP4 gaps through the shared PCM path

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Complete all required codec/container playback and seeking through the shared bounded producer.
Purpose: Measured format gaps use reviewed adapters with the same timing and playback authority.
Output: NativeCodec/DecoderBridge.c, NativeCodec/DecoderBridge.h, MusicPlayer/Audio/XiphPCMDecoder.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayer/Audio/MP4PCMDecoder.swift, docs/codec-matrix.md.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

The source pins and build runner from Plan 11 are prerequisites. New DecoderBridge exposes open/read/seek/format/error/close with caller-owned PCM storage; it performs real C error/channel/time conversion. NEW XiphPCMDecoder conforms directly to PCMDecoder. Conditional NEW MP4PCMDecoder uses AVURLAsset/AVAssetReader audio-only output into the same producer. Build outputs live in ignored build/NativeCodec, not source-controlled binaries.

## Dependencies and scope

Needs Plan 11's implemented capability. This two-task expansion is separated for execution context size; its 8 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 12 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-12-T1: Play and seek missing Ogg formats through bounded native adapters</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>NativeCodec/DecoderBridge.c, NativeCodec/DecoderBridge.h, MusicPlayer/Audio/XiphPCMDecoder.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/CodecMatrixTests.swift</files>
  <behavior>Vorbis/Opus required cases use actual native reads/seeks, corrupt files preserve exact C result, repeated open/close releases handles, chained format changes are explicit and loss policy still forbids stale starts.</behavior>
  <action>For each measured Ogg gap implement the small app-owned C boundary, module import and a real PCMDecoder adapter. Use libvorbisfile PCM reads/seeks and opusfile float reads/op_pcm_seek; Opus seek units are 48 kHz source samples with library preroll. Bound caller capacity and returned frame counts. Surface original result code and operation in DiagnosticRecord. Handle chained links with changing channel count/rate by explicit producer reconfiguration or conversion, not fixed stereo assumptions. Close file/library resources on cancellation and failed preparation; prevent stale generation callbacks entering playback. Update coordinator decoder selection centrally from the measured codec/container map, preserving Apple paths for passing cases and one producer/permission policy. No second graph or play authority. Run existing real codec tests red before adapter implementation, then green; include sanitizer diagnostics on supported simulator builds. Create CodecMatrixTests here for the actual production decoder selection, using the same manifest and recording remaining MP4 cells as outstanding until 01-12-T2; this selected task check must require successful targeted Ogg cases and never mistake Apple-only probe output for native adapter coverage.</action>
  <acceptance_criteria>Every required Ogg cell now opens, produces actual PCM and seeks through the app's shared path; parser errors stay inspectable and repeated operations do not leak or start after loss.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite CodecMatrixTests --suite PlaybackRaceTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Every required Ogg cell now opens, produces actual PCM and seeks through the app's shared path; parser errors stay inspectable and repeated operations do not leak or start after loss.</done>
</task>

<task type="auto" tdd="true">
  <name>01-12-T2: Play MP4 audio correctly and complete the required codec matrix</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Audio/MP4PCMDecoder.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/CodecMatrixTests.swift, MusicPlayerTests/LongRecordingTests.swift, docs/codec-matrix.md</files>
  <behavior>AAC-in-MP4 with video is audio-only; selected audio track follows explicit stable criteria; seeking honors timestamps/edit lists and discards preroll; all nine required cells succeed or the plan remains incomplete.</behavior>
  <action>If AVAudioFile failed AAC MP4, add the Apple-only MP4PCMDecoder using asynchronous AVURLAsset audio-track loading and AVAssetReaderTrackOutput PCM. Select a playable audio track by documented enabled/default preference and stable track ID, never array order. Recreate the reader for each seek before setting timeRange; preserve sample timestamps/edit-list offsets and discard pre-target samples so output/time agree. Bound reads/conversion and report original AVFoundation errors. If the existing Apple path passed, retain it and do not create unused adapter code. Update CodecMatrixTests to assert required final success through the same production coordinator/decoder factory, while AppleCodecMatrixTests remains capability observations. Include long MP3 VBR/Ogg/MP4 landmarks, damaged/truncated files, mixed-rate transition and repeated seek tests, and update the measured matrix per OS. Build script/project membership must consume only actually created adapter sources via the established synchronized groups and native build output settings.</action>
  <acceptance_criteria>CodecMatrixTests rejects any missing/failed required cell; native artifact includes only reviewed needed libraries and all long seek targets are measured. Apple source inspection alone cannot complete this task.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite CodecMatrixTests --suite LongRecordingTests --release</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>CodecMatrixTests rejects any missing/failed required cell; native artifact includes only reviewed needed libraries and all long seek targets are measured. Apple source inspection alone cannot complete this task.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Untrusted compressed audio → reviewed C bridge or Apple reader → shared PCM producer.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-12-2 | Denial of service | Malformed compressed media | high | mitigate | Bound capacities, parser work and cancellation; supported sanitizers and corrupt/chained fixtures. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Required formats all decode and seek through the shared bounded producer; conditional code is adopted only for measured Apple gaps.
Vorbis/Opus chained channel/rate changes and MP4 timestamps/preroll preserve correct source-time seeking and original errors.
No whole-file transcoding or permanent PCM duplication is introduced.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-12-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
