---
phase: 01-installable-native-player
plan: "03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: ["MusicPlayer/Library/TransferDiscovery.swift","MusicPlayer/Persistence/LibraryStore.swift","MusicPlayer/App/AppConfiguration.swift","MusicPlayerTests/TransferDiscoveryTests.swift","MusicPlayer/App/MusicPlayerApp.swift"]
autonomous: true
requirements: ["LIB-01","PLAY-01","PLAY-07"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-13, music transfer uses Apple Devices into app-owned Documents; no in-app audio picker or Add Files flow exists."
    - "Per D-12 through D-15, foreground discovery reconciles contained app-owned regular files in place, persists observations and exposes only verified-ready playable entries; the flat view retains actual relative locators."
    - "Per D-14, scanning preserves existing rows, active loaded revision, audible playback and frozen queue order; repeated scans are idempotent."
    - "LIB-01 concurrency edge: overlapping scans, file growth, overwrite and foreground events serialize revision validation; superseded work cannot publish readiness."
  artifacts:
    - path: "MusicPlayer/Library/TransferDiscovery.swift"
      provides: "Foreground reconciliation, readiness and revision checks"
    - path: "MusicPlayerTests/TransferDiscoveryTests.swift"
      provides: "Changing-file, order and persistence tests"
  key_links:
    - from: "MusicPlayer/App/MusicPlayerApp.swift"
      to: "MusicPlayer/Library/TransferDiscovery.swift"
      via: "scenePhase active starts cancellable scan without issuing playback commands"
    - from: "MusicPlayer/Library/TransferDiscovery.swift"
      to: "MusicPlayer/Persistence/LibraryStore.swift"
      via: "Revision-qualified background transaction and immutable rows"
  prohibitions:
    - statement: "Discovery, restoration and page reopening must never start audio or silently replace the active queue."
      status: "unresolved"
---

# 01-03: Reconcile transferred media without disturbing playback

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Deliver foreground discovery with evidenced readiness, stable identity and preserved listening state.
Purpose: Only unchanged completed revisions become playable while the current queue and audio remain intact.
Output: MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Persistence/LibraryStore.swift, MusicPlayer/App/AppConfiguration.swift, MusicPlayer/App/MusicPlayerApp.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW TransferDiscovery actor: reconcileForeground(), validateRevision(locator:observedRevision:), cancelScan(); ScanSnapshot {readyTracks, pendingTracks, isScanning, failure}. NEW TransferReadiness cases pending, validating, ready(completionEvidence, revision), failed(recordID); use the publication rule actually established in Plan 02. Extend LibraryStore TrackRecord with file resource identity, byteLength, modificationDate, relativeLocator, metadata and validation state; UUID identifies the recording, not its basename. No ready status is inferred from extension alone.

## Dependencies and scope

Needs Plan 02's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 3 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-03-T1: Reconcile transferred media and preserve the current listening session</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Persistence/LibraryStore.swift, MusicPlayer/App/AppConfiguration.swift, MusicPlayerTests/TransferDiscoveryTests.swift, MusicPlayer/App/MusicPlayerApp.swift</files>
  <behavior>Growing file and unchanged decodable interrupted prefix remain pending absent the approved completion evidence; post-parse revision mismatch invalidates validation; repeat scans preserve UUID; replacement cannot inherit recording identity; scan never sends Play or changes active queue.</behavior>
  <action>Implement D-12/D-14/D-15 using Plan 02's observed publication rule; if the user has not resolved an identified required transfer-workflow change, keep that dependent readiness path blocked and surface the recorded question rather than inventing acceptance. Enumerate regular contained Documents files off main, reject symlinks and escapes, and resolve the current Documents root each access. Snapshot resource identity/size/mtime before and after incremental cancellable validation, debounce repeated foreground requests and persist pending observations so relaunch revalidates them. Quiescence only schedules validation. A manifest may be consumed only if that transfer workflow was explicitly authorized at the physical checkpoint; it must never become an implicit requirement. Use bounded file reads and central limits, keep unsupported and corrupt entries separately inspectable, and preserve existing ready rows on scan failure. Do not move or duplicate audio. External replacement retires stale readiness/metadata and does not silently reuse recording identity; if an active open revision cannot remain valid, pause with its original error. Apply completeUntilFirstUserAuthentication to media and Application Support/store sidecars needed while locked, then verify actual next-file behavior later. Persist through a background Core Data context with immutable value snapshots and clear save-failure recovery.</action>
  <acceptance_criteria>Tests reproduce concurrent writes, interruptions, restart, missing files, failed saves and same-name replacement; only the confirmed unchanged revision can become ready. Scans never start audio or replace the active queue.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite TransferDiscoveryTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Tests reproduce concurrent writes, interruptions, restart, missing files, failed saves and same-name replacement; only the confirmed unchanged revision can become ready. Scans never start audio or replace the active queue.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

External file publication → app readiness and background persistent identity.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-03-1 | Tampering | TransferDiscovery | high | mitigate | Contained regular files, before/after revision validation and evidenced completion rule. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-13, music transfer uses Apple Devices into app-owned Documents; no in-app audio picker or Add Files flow exists.
Per D-12 through D-15, foreground discovery reconciles contained app-owned regular files in place, persists observations and exposes only verified-ready playable entries; the flat view retains actual relative locators.
Per D-14, scanning preserves existing rows, active loaded revision, audible playback and frozen queue order; repeated scans are idempotent.
LIB-01 concurrency edge: overlapping scans, file growth, overwrite and foreground events serialize revision validation; superseded work cannot publish readiness.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-03-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
