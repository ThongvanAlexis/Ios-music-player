---
phase: 01-installable-native-player
plan: "15"
type: execute
wave: 15
depends_on: ["01-14"]
files_modified: ["MusicPlayer/Feature/NowPlayingView.swift","MusicPlayer/Feature/TrackTimeFormatter.swift","MusicPlayer/Library/TrackArtworkLoader.swift","MusicPlayer/Feature/AppShell.swift","MusicPlayerUITests/NowPlayingTests.swift","MusicPlayer/Feature/CompactPlayer.swift","MusicPlayerUITests/CompactPlayerTests.swift"]
autonomous: true
requirements: ["PLAY-02","PLAY-03","APP-03","SKIN-01","APP-02"]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-07, every non-Now-Playing page shows the loaded filename and independent compact Play/Pause, and the body opens Now Playing without triggering transport."
    - "Per D-09/D-10, a 64 pt cover sits beside filename-led metadata; timeline and transport receive more space than artwork."
    - "UI empty/now-playing: No selected track shows the documented empty state and Go to Files with no functional-looking inactive transport."
    - "UI loading/now-playing: Preparation shows the target filename, unknown time and Cancel Preparation; seek stays disabled until duration is known."
    - "UI error/now-playing: Failed selection or seek retains technical details and a truthful confirmed paused position."
    - "UI populated/now-playing: A 64 pt cover sits beside filename-led metadata, above time, seek and Previous Play/Pause Next controls."
    - "UI overflow/now-playing: Short-height and large-text player content scrolls without shrinking or overlapping transport targets."
    - "UI long-text/now-playing: Filename and metadata wrap fully; large time labels stack and multi-hour values keep complete digits."
    - "UI empty/compact-player: The compact player is absent with no loaded track and absent on Now Playing."
    - "UI loading/compact-player: Preparation reflects shared state with nonblocking progress and no duplicate start command."
    - "UI error/compact-player: Paused or failed shared playback is reflected in the compact control and reachable warning details."
    - "UI populated/compact-player: Loaded tracks show filename and a separate Play/Pause button; the body opens Now Playing without triggering transport."
    - "UI overflow/compact-player: The compact player expands for accessibility text and remains above the navigation strip without covering content."
    - "UI long-text/compact-player: Ordinary filename uses one middle-truncated line, accessibility uses up to two lines, and its full accessible name opens full player details."
    - "UI loading/seek: Unknown duration disables seek; dragging shows a provisional value and committing shows Seeking until confirmation."
    - "UI error/seek: A failed seek returns to the actual confirmed position, remains paused and exposes Error Details."
    - "UI overflow/seek: The seek target remains within the full-width 44 pt interaction frame; time labels stack when horizontal space is insufficient."
    - "UI long-text/seek: Multi-hour time values remain complete and the accessible Playback position value includes elapsed and total time."
  artifacts:
    - path: "MusicPlayer/Feature/NowPlayingView.swift"
      provides: "Filename, cover, real timeline and transport"
    - path: "MusicPlayer/Feature/CompactPlayer.swift"
      provides: "Shared playback projection with separate tap regions"
  key_links:
    - from: "MusicPlayer/Feature/NowPlayingView.swift"
      to: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      via: "One shared seek/transport/cancel command route"
    - from: "MusicPlayer/Feature/CompactPlayer.swift"
      to: "MusicPlayer/Feature/AppShell.swift"
      via: "Body route separate from play/pause button"
---

# 01-15: Full and compact shared player controls

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Expose truthful shared playback through the full and compact native players.
Purpose: The user can identify, seek and control the same track from every destination.
Output: MusicPlayer/Feature/NowPlayingView.swift, MusicPlayer/Feature/TrackTimeFormatter.swift, MusicPlayer/Library/TrackArtworkLoader.swift, MusicPlayer/Feature/AppShell.swift, MusicPlayer/Feature/CompactPlayer.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW NowPlayingView reads immutable PlaybackSnapshot and local provisional drag position; no independent playback clock. NEW TrackTimeFormatter uses m:ss/h:mm:ss and unknown --:--. NEW TrackArtworkLoader performs bounded image extraction/downsampling off main and returns actual metadata or shared fallback. NEW CompactPlayer receives shared snapshot and navigation action; body and 48 pt transport are sibling controls.

## Dependencies and scope

Needs Plan 14's implemented capability. This two-task expansion is separated for execution context size; its 7 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 15 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-15-T1: Render the full player with accessible seeking and truthful state</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Feature/NowPlayingView.swift, MusicPlayer/Feature/TrackTimeFormatter.swift, MusicPlayer/Library/TrackArtworkLoader.swift, MusicPlayer/Feature/AppShell.swift, MusicPlayerUITests/NowPlayingTests.swift</files>
  <behavior>No track hides transport and offers Go to Files; preparation exposes Cancel; unknown duration disables seek; long Unicode metadata wraps; drag time is provisional; accessible adjustment moves ten seconds clamped without tick announcements.</behavior>
  <action>Build Now Playing with a 64 pt cover beside fully wrapping filename and optional title/artist per D-09/D-10. Extract/downsample actual embedded artwork asynchronously with configured byte/pixel limits; absence uses shared music-note artwork, malformed art produces diagnostic without making playable audio fail. Use approved elapsed Display/total Label styles, monospaced digits and full m:ss/h:mm:ss values; stack labels at accessibility sizes. Seek has a 44 pt interaction frame, 8 pt visible track and 24 pt thumb, with local drag preview that does not fight render ticks; release commits an absolute shared seek and shows Seeking until confirmed. VoiceOver Playback position exposes elapsed/total and 10-second adjustments. Space transport 32 pt below, 48/64/48 pt targets and 24 pt gaps; disable boundary controls. Render all preparation/cancel/paused/playing/seek error/queue end/interruption/restoration statuses from AppCopy and the shared snapshot. Keep content scrollable rather than shrinking controls; navigation remains visible. No waveform or other later-feature control is shown.</action>
  <acceptance_criteria>NowPlayingTests cover every lifted now-playing/seek criterion, exact copy, separate touch frames, unknown duration and actual shared transport commands.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite SeekTests --ui-suite NowPlayingTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>NowPlayingTests cover every lifted now-playing/seek criterion, exact copy, separate touch frames, unknown duration and actual shared transport commands.</done>
</task>

<task type="auto" tdd="true">
  <name>01-15-T2: Control the same playback from the compact player on all other pages</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Feature/CompactPlayer.swift, MusicPlayer/Feature/AppShell.swift, MusicPlayerUITests/CompactPlayerTests.swift</files>
  <behavior>Loaded track appears everywhere except Now Playing; no track hides compact UI; body opens full player only; separate button toggles exactly once; loading/errors/loss reflect shared state and large text expands safely.</behavior>
  <action>Implement D-07 with the shared skin's Secondary surface, 16 pt inset and 64 pt minimum height. The filename body is one button labeled Open Now Playing, {filename}; the trailing 48 pt Play/Pause control is a separate sibling tap region. Keep one middle-truncated line at ordinary text sizes and up to two expanding lines at accessibility sizes, with full accessible filename. Read only shared state/time and dispatch shared commands, disable duplicate starts while preparing and show nonblocking progress without percentage. Show the warning/status above the compact player and keep both above the fixed navigation inset on Files, Settings and all later-feature destinations. Opening player/details never changes playback or clears loss inhibition. Verify loss/seek error/failed selection all show matching paused state.</action>
  <acceptance_criteria>CompactPlayerTests prove body-versus-transport separation, single command dispatch, exact shared state and no covered navigation/content under warning plus large-text compact player.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --ui-suite CompactPlayerTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>CompactPlayerTests prove body-versus-transport separation, single command dispatch, exact shared state and no covered navigation/content under warning plus large-text compact player.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Shared confirmed playback and embedded artwork/text → native full and compact player controls.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-15-2 | Denial of service | Artwork/long text/time values | medium | mitigate | Bound/downsample images, validate seek numbers and scroll native text. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-07, every non-Now-Playing page shows the loaded filename and independent compact Play/Pause, and the body opens Now Playing without triggering transport.
Per D-09/D-10, a 64 pt cover sits beside filename-led metadata; timeline and transport receive more space than artwork.
UI empty/now-playing: No selected track shows the documented empty state and Go to Files with no functional-looking inactive transport.
UI loading/now-playing: Preparation shows the target filename, unknown time and Cancel Preparation; seek stays disabled until duration is known.
UI error/now-playing: Failed selection or seek retains technical details and a truthful confirmed paused position.
UI populated/now-playing: A 64 pt cover sits beside filename-led metadata, above time, seek and Previous Play/Pause Next controls.
UI overflow/now-playing: Short-height and large-text player content scrolls without shrinking or overlapping transport targets.
UI long-text/now-playing: Filename and metadata wrap fully; large time labels stack and multi-hour values keep complete digits.
UI empty/compact-player: The compact player is absent with no loaded track and absent on Now Playing.
UI loading/compact-player: Preparation reflects shared state with nonblocking progress and no duplicate start command.
UI error/compact-player: Paused or failed shared playback is reflected in the compact control and reachable warning details.
UI populated/compact-player: Loaded tracks show filename and a separate Play/Pause button; the body opens Now Playing without triggering transport.
UI overflow/compact-player: The compact player expands for accessibility text and remains above the navigation strip without covering content.
UI long-text/compact-player: Ordinary filename uses one middle-truncated line, accessibility uses up to two lines, and its full accessible name opens full player details.
UI loading/seek: Unknown duration disables seek; dragging shows a provisional value and committing shows Seeking until confirmation.
UI error/seek: A failed seek returns to the actual confirmed position, remains paused and exposes Error Details.
UI overflow/seek: The seek target remains within the full-width 44 pt interaction frame; time labels stack when horizontal space is insufficient.
UI long-text/seek: Multi-hour time values remain complete and the accessible Playback position value includes elapsed and total time.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-15-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
