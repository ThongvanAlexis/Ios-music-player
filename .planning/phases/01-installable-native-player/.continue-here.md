---
phase: 01-installable-native-player
plan: "01"
task: 2
total_tasks: 2
status: paused
checkpoint_type: human-action
updated: "2026-09-12"
---

# Resume the first native build checkpoint

The first native slice is authored and locally reviewed. The build tooling and
native source have local commits on `main`. No push was performed by the agent.
No native test, Release build, physical installation or audio result is passed.
Do not restart implementation from scratch or create a completed SUMMARY.

## Prepared work

- Native iOS 17 / Swift 6 app, app-hosted tests, UI tests and shared Xcode scheme.
- Real background Core Data/SQLite identity, contained Documents file discovery,
  closed PCM WAV validation, persisted pre/post transfer observations.
- One bounded AVAudioEngine player, synchronous route-loss inhibition and
  explicit Play recovery; source review fixed a session-notification lock risk
  and Pause during preparation.
- Files rows, dark JSON/image skin, playback snapshots and selectable/copyable
  file/error details. Unsupported files stay inspectable and pending.
- Pinned official CI actions, current-source simulator/Release wrapper, IPA
  manifest/checksum/results and portable evidence validation.
- 23 Python tooling tests passed. Source/structural checks do not replace Xcode.

## Current blocker and user action

The user must push the prepared `main` tip to the configured origin
`https://github.com/ThongvanAlexis/Ios-music-player.git`. The workflow runs on
that push. The agent must never push. The exact final tip is provided in the
conversation; discover it with `git rev-parse HEAD` if resuming from disk.

## Resume procedure

1. Confirm the user has pushed the prepared source and identify its exact SHA.
2. Use `gh run list/view` for `ci.yml` on that SHA, inspect actual macOS logs,
   and download the selected run into a new temporary directory. Keep originals.
3. Successful artifacts contain `artifact/` (IPA, manifest, checksum and native
   evidence) and `results/` (xcresult bundles and logs). Copy only the selected
   report to `build/native-evidence.json` after checking identity.
4. Run `python scripts/native_check.py --check-evidence build/native-evidence.json --expected-sha HEAD`.
   Read failures; fix source locally if needed, then request another user push.
   Do not accept stale source, absent classes, zero tests or source inspection
   as native success. Fixture preservation across Xcode launches and all Swift
   compilation/runtime behavior still require the real run.
5. Complete Task 1 native acceptance and Task 2 only after matching successful
   simulator and unsigned Release evidence. Then write 01-01-SUMMARY.md, run
   the wave checks, and continue Plan 02 for the downloader and actual phone/USB
   observations. All 19 later plans remain dependent on this unfinished plan.

## Scope notes

No external app package was installed. Official action input review is in
`docs/build-input-review.md`. Device observations remain blank/pending in
`docs/device-validation.md`. The MP3 prefix setup establishes inspectable
pending bytes; it does not claim measured MP3 decoding or transfer completion.
Only the initial WAV path is implemented; later navigation, codecs, transport
and diagnostic persistence retain their planned scope.
