---
phase: 01-installable-native-player
plan: "17"
type: execute
wave: 17
depends_on: ["01-16"]
files_modified: ["MusicPlayer/Library/TransferDiscovery.swift","MusicPlayer/Persistence/LibraryStore.swift","MusicPlayerTests/LockedFileAccessTests.swift","MusicPlayerUITests/BackgroundStateTests.swift","docs/device-validation.md"]
autonomous: true
requirements: ["PLAY-04","PLAY-05","PLAY-06"]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "After first device unlock, a previously unopened queued file and required database sidecars remain available when the phone is locked."
    - "Background/foreground views reflect actual shared playback state; locked-access errors pause and preserve queue and original causes."
    - "Per D-23, physical locked next-file and database checks remain pending until final acceptance on the installed source SHA."
  artifacts:
    - path: "MusicPlayerTests/LockedFileAccessTests.swift"
      provides: "Protection and unopened-file integration checks"
  key_links:
    - from: "MusicPlayer/Persistence/LibraryStore.swift"
      to: "MusicPlayer/Library/TransferDiscovery.swift"
      via: "Required media/store/sidecar protection during locked progression"
---

# 01-17: Locked-file access and background state checks

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Keep newly opened queued media and required storage available while the phone is locked.
Purpose: Background playback must not rely on a file already being open before locking.
Output: MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Persistence/LibraryStore.swift, docs/device-validation.md.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

Existing protection configuration uses completeUntilFirstUserAuthentication for Documents media and needed Application Support database, WAL/SHM and resume files. NEW LockedFileAccessTests and BackgroundStateTests check reopen/protection/state behavior; docs/device-validation.md retains pending physical steps.

## Dependencies and scope

Needs Plan 16's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 17 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-17-T1: Keep newly opened media available through locked queue transitions</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Persistence/LibraryStore.swift, MusicPlayerTests/LockedFileAccessTests.swift, MusicPlayerUITests/BackgroundStateTests.swift, docs/device-validation.md</files>
  <behavior>Media and DB sidecars receive intended protection; previously unopened next file resolves under current root; coming foreground displays actual shared state; locked-access failure pauses/reports without erasing queue or causes.</behavior>
  <action>Verify and finish completeUntilFirstUserAuthentication protection across existing and newly discovered media and required DB/resume/diagnostic sidecars, using background workers. Do not depend on an already open file descriptor as proof for next-file access. Add integration tests that close/reopen media and store after applying protection, verify metadata values and preserve original errors on denial. Add UI background/foreground tests that show actual playback/paused state and do not infer it from old view state. Extend the physical procedure with first unlock, start A, lock, then automatically open previously unopened B; repeat with remote Next/Previous/seek, all format paths, and loss near the A/B boundary. Check a store write/read while locked when needed. Actual locked access and speaker silence are completed in the final device acceptance on the final SHA, not by simulator property assertions.</action>
  <acceptance_criteria>Automated protection/reopen/shared-state tests pass; the device record has precise steps and pending fields for locked-next-file evidence. PLAY-04/05/06 remain incomplete until the final physical results pass.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite LockedFileAccessTests --ui-suite BackgroundStateTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
    <human-check>On the final iPhone build, opening a previously unopened next track while locked must succeed and loss/reconnect interleavings must remain silent until explicit Play.</human-check>
  </verify>
  <done>Automated protection/reopen/shared-state tests pass; the device record has precise steps and pending fields for locked-next-file evidence. PLAY-04/05/06 remain incomplete until the final physical results pass.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Lock-state file protection → unopened decoder files, persistent sidecars and restored foreground UI.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-17-2 | Denial of service | Locked file/DB access | high | mitigate | Correct protection for unopened media and sidecars; actual locked read/write and recovery checks. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
After first device unlock, a previously unopened queued file and required database sidecars remain available when the phone is locked.
Background/foreground views reflect actual shared playback state; locked-access errors pause and preserve queue and original causes.
Per D-23, physical locked next-file and database checks remain pending until final acceptance on the installed source SHA.
</success_criteria>

## Flagged assumptions

EDGE PLAY-04/unclassified remain unresolved assumptions tied to physical locked/media-control/audio-route evidence. Property inspection and simulated notifications do not resolve the physical claims.

<output>
Create .planning/phases/01-installable-native-player/01-17-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
