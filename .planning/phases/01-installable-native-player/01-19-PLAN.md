---
phase: 01-installable-native-player
plan: "19"
type: execute
wave: 19
depends_on: ["01-18"]
files_modified: ["MusicPlayer/Diagnostic/DiagnosticStore.swift","MusicPlayer/Diagnostic/DiagnosticFileWriter.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayerTests/DiagnosticStoreTests.swift","MusicPlayerTests/DiagnosticFlowTests.swift"]
autonomous: true
requirements: ["APP-02","PLAY-01","LIB-01"]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-25/D-26, discovery/preparation/playback/seek/restore/skip failures retain operation, affected file, original message/domain/code and underlying causes, with recovery action."
    - "Per D-26/D-27, normal history remains inspectable/copyable and persists across relaunch even when optional file logging is disabled; file writes run off main/render threads."
    - "Repeated/cyclic errors and failed persistence or optional logging remain bounded and inspectable without recursive failure reporting."
  artifacts:
    - path: "MusicPlayer/Diagnostic/DiagnosticFileWriter.swift"
      provides: "Optional serial bounded file logging"
  key_links:
    - from: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      via: "Retain original causes and operation-specific recovery for every failure"
    - from: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticFileWriter.swift"
      via: "Optional injected writer cannot replace or block normal retained history"
---

# 01-19: Complete diagnostic causes and optional file logging

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Retain full causes across every failing operation with independent optional file logging.
Purpose: Normal diagnostic history remains available after successful playback and relaunch even when verbose logging fails or is disabled.
Output: MusicPlayer/Diagnostic/DiagnosticStore.swift, MusicPlayer/Diagnostic/DiagnosticFileWriter.swift, MusicPlayer/Audio/PlaybackCoordinator.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW DiagnosticFileWriter actor is an optional serial bounded rotating JSON-lines sink. DiagnosticStore persistence remains the normal history and cannot depend on that optional sink. Existing coordinator and error producers enrich the same original record with operation and recovery.

## Dependencies and scope

Needs Plan 18's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 19 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-19-T1: Preserve full error causes across every failing operation and optional logging</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Diagnostic/DiagnosticStore.swift, MusicPlayer/Diagnostic/DiagnosticFileWriter.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/DiagnosticStoreTests.swift, MusicPlayerTests/DiagnosticFlowTests.swift</files>
  <behavior>Each operation records its original cause/recovery; disabled verbose writer still persists normal history; writer failures cannot recurse or block playback; cancellation/supersession is distinguishable from actual error; retention limits apply.</behavior>
  <action>Audit and finish the single diagnostic path across discovery, direct preparation, automatic skips, decode/render failures, seek, restored missing media and skin failure. Preserve original NSError underlying chains and native result codes, including failed-operation and affected-file context, instead of overwriting them with generic text. When a lower layer returns a failure, enrich the record with recovery and operation without losing the cause. Coalesce repeated identical failures with a count and last occurrence, apply bounded recent-record retention and cause depth, and preserve surviving records across successful playback/page changes/relaunch. Keep history persistence on DiagnosticStore's worker and optional verbose file logging on a separate injected serial writer, disabled by default and absent from Settings until a real control is in scope. The writer adds rotation/bounded size/error handling and never runs in render callback or on main. Handle full disk/permission failures with in-memory inspectable records and one bounded writer-failure record, avoiding recursive logging.</action>
  <acceptance_criteria>DiagnosticStoreTests/DiagnosticFlowTests force each listed failure, disabled logging, failed persistence and long cause chains; full original details remain inspectable and the player thread never waits on disk I/O.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite DiagnosticStoreTests --suite DiagnosticFlowTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>DiagnosticStoreTests/DiagnosticFlowTests force each listed failure, disabled logging, failed persistence and long cause chains; full original details remain inspectable and the player thread never waits on disk I/O.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

OS/decoder failures → bounded diagnostic history; optional serial sink → disk.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-19-1 | Denial of service | Repeated errors/cause cycles/disk failure | high | mitigate | Bound traversal/retention/rotation and prevent recursive logging; worker I/O only. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-25/D-26, discovery/preparation/playback/seek/restore/skip failures retain operation, affected file, original message/domain/code and underlying causes, with recovery action.
Per D-26/D-27, normal history remains inspectable/copyable and persists across relaunch even when optional file logging is disabled; file writes run off main/render threads.
Repeated/cyclic errors and failed persistence or optional logging remain bounded and inspectable without recursive failure reporting.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-19-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
