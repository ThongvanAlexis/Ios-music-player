---
phase: 01-installable-native-player
plan: "09"
type: execute
wave: 9
depends_on: ["01-08"]
files_modified: ["MusicPlayer/Audio/PCMDecoder.swift","MusicPlayer/Audio/ApplePCMDecoder.swift","MusicPlayer/Audio/PCMProducer.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayerTests/PCMProducerTests.swift","MusicPlayer/Domain/PlaybackPolicy.swift","MusicPlayerTests/PlaybackPolicyTests.swift","MusicPlayerTests/PlaybackRaceTests.swift","MusicPlayer/App/AppConfiguration.swift"]
autonomous: true
requirements: ["PLAY-01","PLAY-03","PLAY-06","PLAY-02"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-21/D-22, headphone-loss inhibition survives selection, Next, seek, queue end, restoration, reconnection and stale callbacks until explicit app/system Play."
    - "Per D-24, other interruption resume requires previous playing intent, iOS permission and no later Pause/Cancel/loss."
    - "Audio uses bounded reusable PCM buffers, source-time accounting and one graph across formats; memory does not scale with entire recording duration."
    - "PLAY-02 concurrency edge: only the current request generation may change loaded track, confirmed time, queue or playback intent."
  artifacts:
    - path: "MusicPlayer/Audio/PCMDecoder.swift"
      provides: "Meaningful swappable decoder boundary"
    - path: "MusicPlayer/Audio/PCMProducer.swift"
      provides: "Bounded scheduling and audible-end accounting"
    - path: "MusicPlayer/Domain/PlaybackPolicy.swift"
      provides: "Serialized command/permission state transitions"
    - path: "MusicPlayerTests/PlaybackPolicyTests.swift"
      provides: "Adversarial event ordering and start count assertions"
  key_links:
    - from: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      to: "MusicPlayer/Domain/PlaybackPolicy.swift"
      via: "Every graph start asks current permission immediately before start"
    - from: "MusicPlayer/Audio/PCMProducer.swift"
      to: "MusicPlayer/Audio/PCMDecoder.swift"
      via: "Bounded caller-owned buffer and generation-tagged results"
  prohibitions:
    - statement: "Headphone loss must never lead to audible restart until explicit app or system Play, including after reconnection or delayed callbacks."
      status: "unresolved"
---

# 01-09: Bounded audio production and one start authority

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Extend the real audio path to sustained long-recording playback without unbounded memory or stale starts.
Purpose: All later codecs and UI commands share the same source timing and headphone-loss precedence.
Output: MusicPlayer/Audio/PCMDecoder.swift, MusicPlayer/Audio/PCMProducer.swift, MusicPlayer/Domain/PlaybackPolicy.swift, MusicPlayerTests/PlaybackPolicyTests.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW PCMDecoder protocol {format, durationFrames, read(into:), seek(toSourceFrame:), close()}; implementations own actual cursor/format/error conversion rather than mirroring framework methods. NEW ApplePCMDecoder implements the boundary using AVAudioFile. NEW PCMProducer owns a fixed pool, decode backpressure, source/rendered/queued frames, immutable completion events and source generation. NEW PlaybackPolicy value state defines PlaybackCommand (explicitPlay, pause, select, previous, next, seek, cancelPreparation, restore), PreparationState, userIntentGeneration, operationGeneration, requiresExplicitPlay and interruption snapshot. Coordinator remains sole graph/start authority. Default 4096-frame buffers and max four scheduled buffers are configurable limits, not performance measurements.

## Dependencies and scope

Needs the preceding plan's implemented capability; creates the artifacts below. Sequential wave 9 follows shared-file ownership and the project's sequential execution setting.

<tasks>
<task type="auto" tdd="true">
  <name>01-09-T1: Play long files through reusable buffers and correct audible completion</name>
  <reversibility rating="costly">All required decoders share the source-frame interface and bounded producer; conversion stays behind this boundary.</reversibility>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Audio/PCMDecoder.swift, MusicPlayer/Audio/ApplePCMDecoder.swift, MusicPlayer/Audio/PCMProducer.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/PCMProducerTests.swift</files>
  <behavior>A long generated source never exceeds configured pool/queued frames; backpressure halts reads; consumed callbacks only recycle; played-back end advances once; stop/seek callbacks from prior generations cannot advance.</behavior>
  <action>Extract the working Apple file cursor into a real PCMDecoder implementation, preserving the existing audible path. Add reusable buffer ownership and bounded prefetch/conversion on an audio worker. Use AVAudioConverter when source/output formats differ, with explicit buffer sizing and errors. Never retain full compressed audio or PCM and never create permanent converted copies. Render callbacks emit small generation-tagged events only; database, logging and file reads never run there. Distinguish data-consumed reuse from data-played-back completion, EOF discovery from heard EOF, and source frames from output device frames. A stopped node's callbacks cannot be interpreted as successful song completion. Reconfigure safely across channel/rate changes; report unsupported graph formats with original cause. Keep one audio graph with a natural mixer insertion point for later EQ but add no EQ processing.</action>
  <acceptance_criteria>PCMProducerTests assert bounded read-ahead/pool ownership, mixed sample rates/channels, cancellation and heard-end semantics; the existing native tracer still produces audio through the new path.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite PCMProducerTests --suite NativeTracerTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>PCMProducerTests assert bounded read-ahead/pool ownership, mixed sample rates/channels, cancellation and heard-end semantics; the existing native tracer still produces audio through the new path.</done>
</task>

<task type="auto" tdd="true">
  <name>01-09-T2: Make every automatic start obey current user intent and loss inhibition</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Domain/PlaybackPolicy.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/PlaybackPolicyTests.swift, MusicPlayerTests/PlaybackRaceTests.swift, MusicPlayer/App/AppConfiguration.swift</files>
  <behavior>Permute loss/reconnect/interruption-end/prepare-complete/seek-complete/EOF/Next/selection; only explicit app/system Play clears loss. Pause and Cancel invalidate older intent; duplicate Play during prepare cannot start twice; timeouts reject late results.</behavior>
  <action>Implement the full single-authority policy per D-21/D-22/D-24. On headphone loss first latch requiresExplicitPlay and invalidate pending start authorization, then pause/capture confirmed position. Reconnection only updates route facts. Explicit Play is the only command that clears the latch; selecting a file or using Previous/Next/seek after loss may prepare/change position but remains paused. Store playing-at-interruption-start and intent generation; resume only when iOS allows, intent still matches and no loss/later pause/cancel intervened. Recheck permission and generation directly at every scheduling/start commit, including prepare completion, seek completion, EOS progression and graph reconstruction. Add bounded preparation/seek timeouts from shared config and Cancel Preparation returning to the previous loaded track paused or empty; stale completions cannot rename the current track. Use controllable fakes with meaningful start/schedule counters and enumerate event-order permutations, including route loss immediately before next file start. UI projects coordinator state rather than owning timers or playback flags.</action>
  <acceptance_criteria>Policy/race tests prove zero start calls for every forbidden path until explicit Play, exactly one authorized start, current-only filename/time changes, and bounded timeout/cancel behavior.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite PlaybackPolicyTests --suite PlaybackRaceTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Policy/race tests prove zero start calls for every forbidden path until explicit Play, exactly one authorized start, current-only filename/time changes, and bounded timeout/cancel behavior.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

UI/system/route callbacks and decoder workers → serialized playback state → graph start.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-09-1 | Information disclosure | Unexpected speaker output | high | mitigate | Current permission and generation checked at the only start site; event permutations plus required physical checks. |
| T-01-09-2 | Denial of service | Long-file decoder/scheduler | high | mitigate | Fixed buffer pool/backpressure and no blocking work on render/main thread. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-21/D-22, headphone-loss inhibition survives selection, Next, seek, queue end, restoration, reconnection and stale callbacks until explicit app/system Play. Per D-24, other interruption resume requires previous playing intent, iOS permission and no later Pause/Cancel/loss. Audio uses bounded reusable PCM buffers, source-time accounting and one graph across formats; memory does not scale with entire recording duration. PLAY-02 concurrency edge: only the current request generation may change loaded track, confirmed time, queue or playback intent.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-09-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
