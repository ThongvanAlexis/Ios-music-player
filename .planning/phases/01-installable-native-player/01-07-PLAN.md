---
phase: 01-installable-native-player
plan: "07"
type: execute
wave: 7
depends_on: ["01-06"]
files_modified: ["MusicPlayer/Feature/AppShell.swift","MusicPlayer/Feature/UnavailableFeatureView.swift","MusicPlayer/Feature/SettingsView.swift","MusicPlayer/App/MusicPlayerApp.swift","MusicPlayerUITests/NavigationTests.swift"]
autonomous: true
requirements: ["APP-02","APP-03","SKIN-01"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-04/D-05, all eight icon-only destinations remain in fixed order with separate at least 44 pt targets at 375 pt, exact accessible names and a selected marker."
    - "Per D-06, first launch selects Files and later launches restore the last page without sound; reselecting a page preserves its state."
    - "APP-03 empty/encoding/concurrency edges: every destination has real heading/copy, full accessible names, and usable navigation during loading/errors without granting audio intent."
    - "UI loading/navigation: All eight destinations remain enabled during discovery, preparation and restoration."
    - "UI error/navigation: A discovery or playback error leaves navigation usable and retains an Error Details affordance."
    - "UI overflow/navigation: All eight icons occupy separate slots in one fixed row at 375 pt and wider supported layouts; content scrolls above the bottom inset."
    - "UI long-text/navigation: Page headings wrap; full accessible destination names remain available while the strip remains icon-only."
    - "UI loading/placeholder-settings: Bundled placeholder content and navigation remain immediately available while shared playback or diagnostics loads."
    - "UI error/placeholder-settings: Playback warnings remain reachable from later-feature pages and Settings; failed skin loading preserves an emergency readable appearance."
    - "UI overflow/placeholder-settings: Placeholder and Settings content scrolls above the compact player and fixed navigation without clipped actions."
    - "UI long-text/placeholder-settings: Headings and explanatory copy wrap at accessibility text sizes; Go to Files and View Diagnostics remain separately reachable."
  artifacts:
    - path: "MusicPlayer/Feature/AppShell.swift"
      provides: "Fixed native eight-destination shell"
    - path: "MusicPlayer/Feature/SettingsView.swift"
      provides: "Dark appearance and diagnostics route"
    - path: "MusicPlayerUITests/NavigationTests.swift"
      provides: "Navigation/state/accessibility tests"
  key_links:
    - from: "MusicPlayer/Feature/AppShell.swift"
      to: "MusicPlayer/Persistence/LibraryStore.swift"
      via: "Restores and saves selected NavigationDestination"
    - from: "MusicPlayer/Feature/SettingsView.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      via: "View Diagnostics uses retained records"
---

# 01-07: Eight fixed destinations and silent page restoration

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Make all eight destinations reachable in one fixed icon row and restore the last page silently.
Purpose: Navigation stays available during playback, preparation, discovery and errors.
Output: MusicPlayer/Feature/AppShell.swift, MusicPlayer/Feature/UnavailableFeatureView.swift, MusicPlayer/Feature/SettingsView.swift, MusicPlayer/App/MusicPlayerApp.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW NavigationDestination enum is declared in AppShell.swift and centralizes ordered cases, AppCopy headings, accessible names and SkinRole mappings. NEW AppShell persists selected destination and composes a bottom safe-area inset. NEW UnavailableFeatureView uses specific approved copy and Go to Files for Tags, Playlists, Favorite Songs, Favorite Moments and Equalizer. NEW SettingsView shows Appearance / Dark explanation and diagnostics entry.

## Dependencies and scope

Needs Plan 06's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 7 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-07-T1: Navigate all eight pages and restore the last page</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Feature/AppShell.swift, MusicPlayer/Feature/UnavailableFeatureView.swift, MusicPlayer/Feature/SettingsView.swift, MusicPlayer/App/MusicPlayerApp.swift, MusicPlayerUITests/NavigationTests.swift</files>
  <behavior>Eight ordered accessible buttons remain enabled during scan/preparation/failure; first launch Files and subsequent page restoration are silent; repeated selection retains view state; unavailable feature routes expose only Go to Files and functional shared playback.</behavior>
  <action>Implement D-04/D-05/D-06 with a concrete SwiftUI AppShell. Use folder, tag, music.note, list.bullet, composite favorite images, slider.horizontal.3 and gearshape in that exact order; verify minimum-OS symbol availability and retain meanings via bundled assets if any symbol is unavailable. Use 56 pt navigation height, 8 pt outer horizontal insets, zero gap and eight equal slots; at 375 pt each is 44.875 pt with no overlapping contentShape. Show 24 pt icons, a 4 pt selected marker and selected accessibility trait. Headings wrap and use AppCopy. Place scroll content above status, eligible compact-player insertion point, bar and safe inset. Render the five later-feature destinations with their exact unavailable text and Go to Files; no demo records or controls for unimplemented features. Settings shows the actual dark appearance and opens current diagnostics. Bind initial/last destination through the store and never issue playback from restoration. Keep page state when reselected and navigation available through every shared loading/error state.</action>
  <acceptance_criteria>NavigationTests measure separate frames for eight buttons, correct labels/order/traits, page restoration without starts, wrapping headings and usable routes while delayed background work completes. At this slice, Now Playing renders the real loaded filename and shared play/pause control directly in AppShell, or its approved no-track content; Settings renders the existing retained diagnostics as selectable text. Plans 15 and 18 expand these real paths into the dedicated player and diagnostic views.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --ui-suite NavigationTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>NavigationTests measure separate frames for eight buttons, correct labels/order/traits, page restoration without starts, wrapping headings and usable routes while delayed background work completes.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Persisted page identity and shared loading/error state → native navigation.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-07-1 | Tampering | Persisted destination | medium | mitigate | Resolve only centralized NavigationDestination cases, default safely and never grant playback intent from restoration. |
| T-01-07-2 | Denial of service | Loading state and long headings | medium | mitigate | Keep all eight routes usable and scroll wrapping content without overlapping controls. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-04/D-05, all eight icon-only destinations remain in fixed order with separate at least 44 pt targets at 375 pt, exact accessible names and a selected marker.
Per D-06, first launch selects Files and later launches restore the last page without sound; reselecting a page preserves its state.
APP-03 empty/encoding/concurrency edges: every destination has real heading/copy, full accessible names, and usable navigation during loading/errors without granting audio intent.
UI loading/navigation: All eight destinations remain enabled during discovery, preparation and restoration.
UI error/navigation: A discovery or playback error leaves navigation usable and retains an Error Details affordance.
UI overflow/navigation: All eight icons occupy separate slots in one fixed row at 375 pt and wider supported layouts; content scrolls above the bottom inset.
UI long-text/navigation: Page headings wrap; full accessible destination names remain available while the strip remains icon-only.
UI loading/placeholder-settings: Bundled placeholder content and navigation remain immediately available while shared playback or diagnostics loads.
UI error/placeholder-settings: Playback warnings remain reachable from later-feature pages and Settings; failed skin loading preserves an emergency readable appearance.
UI overflow/placeholder-settings: Placeholder and Settings content scrolls above the compact player and fixed navigation without clipped actions.
UI long-text/placeholder-settings: Headings and explanatory copy wrap at accessibility text sizes; Go to Files and View Diagnostics remain separately reachable.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-07-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
