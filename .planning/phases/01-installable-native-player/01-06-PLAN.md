---
phase: 01-installable-native-player
plan: "06"
type: execute
wave: 6
depends_on: ["01-05"]
files_modified: ["MusicPlayer/Theme/Skin.swift","MusicPlayer/Resources/Skins/Dark/skin.json","MusicPlayer/Resources/Skins/Dark/favorite-songs.png","MusicPlayer/Resources/Skins/Dark/favorite-moments.png","MusicPlayerTests/SkinTests.swift"]
autonomous: true
requirements: ["SKIN-01","APP-02"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-08/D-11, Dark loads through the shared versioned JSON/image role model; native code owns geometry and behavior, and later skins remain in Phase 7."
    - "Actual bundled skin JSON/images validate through one shared loader, and missing optional images use declared fallbacks."
    - "Invalid required skin data retains the original diagnostic and provides readable native controls with bounded image decoding."
  artifacts:
    - path: "MusicPlayer/Theme/Skin.swift"
      provides: "Shared semantic asset/state model and loader"
  key_links:
    - from: "MusicPlayer/Theme/Skin.swift"
      to: "MusicPlayer/Resources/Skins/Dark/skin.json"
      via: "Single role/state schema consumed by native controls"
---

# 01-06: Shared image-backed dark skin roles

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Render the shared Dark JSON/image appearance through native controls.
Purpose: All later controls and skins share one versioned role/state model while native code owns layout and actions.
Output: MusicPlayer/Theme/Skin.swift, MusicPlayer/Resources/Skins/Dark/skin.json, MusicPlayer/Resources/Skins/Dark/favorite-songs.png, MusicPlayer/Resources/Skins/Dark/favorite-moments.png.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

Extend existing Skin.swift with version, semantic colors, spacing/type roles, surface/frame, navigation normal/selected/pressed/disabled, artwork fallback, transport and timeline roles; asset keys are declared once in Swift and JSON decoding validates them. JSON contains no executable behavior or arbitrary coordinates.

## Dependencies and scope

Needs Plan 05's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 6 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-06-T1: Render real shared image-backed skin roles in native controls</name>
  <reversibility rating="costly">Shared appearance roles are consumed by later bundled/imported skins; a versioned definition and one loader localize future migration.</reversibility>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Theme/Skin.swift, MusicPlayer/Resources/Skins/Dark/skin.json, MusicPlayer/Resources/Skins/Dark/favorite-songs.png, MusicPlayer/Resources/Skins/Dark/favorite-moments.png, MusicPlayerTests/SkinTests.swift</files>
  <behavior>Bundled JSON resolves every required role/state and real image; missing optional image uses declared fallback; invalid required data gives readable emergency appearance plus original diagnostic; image lookup rejects escapes and excessive decoded pixels.</behavior>
  <action>Expand the actual Dark loader per D-08/D-11 and the approved Shared Skin Roles table. Use the exact approved colors, authored type sizes/weights, spacing, radii and state roles, with native scaled metrics and no cap on Dynamic Type. Decode and validate JSON off main, cache bounded images, centralize role/state names, and use the same loader for all assets. Create distinguishable project-owned star/music-note and star/hourglass composite PNG assets at appropriate scales; these are image roles used by the real buttons, not invented SF Symbol names. Support appearance of normal, selected, pressed and disabled controls through the declared role model. Missing optional images use declared system/color fallback; invalid built-in data records a diagnostic and selects one app-owned emergency readable appearance. Native geometry/hit areas/accessibility/actions are never read from skin JSON. Do not add switching, other bundled skins or archive handling.</action>
  <acceptance_criteria>SkinTests load actual bundled JSON/images, cover invalid schema/version/role/image bounds and prove the first rendered native controls use the shared values rather than per-view palettes.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite SkinTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>SkinTests load actual bundled JSON/images, cover invalid schema/version/role/image bounds and prove the first rendered native controls use the shared values rather than per-view palettes.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Bundled JSON and images → shared native appearance.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-06-1 | Tampering | SkinLoader asset paths | high | mitigate | Validate version/roles, containment and decoded image bounds; readable diagnostic fallback. |
| T-01-06-2 | Denial of service | Large image or text | medium | mitigate | Bound image decoding and use scrolling/wrapping native text without blocking audio. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-08/D-11, Dark loads through the shared versioned JSON/image role model; native code owns geometry and behavior, and later skins remain in Phase 7.
Actual bundled skin JSON/images validate through one shared loader, and missing optional images use declared fallbacks.
Invalid required skin data retains the original diagnostic and provides readable native controls with bounded image decoding.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-06-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
