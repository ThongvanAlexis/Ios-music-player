---
phase: 01-installable-native-player
plan: "14"
type: execute
wave: 14
depends_on: ["01-13"]
files_modified: ["MusicPlayer/Audio/SourceTimeline.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayer/Audio/PCMProducer.swift","MusicPlayerTests/SeekTests.swift","MusicPlayerTests/LongRecordingTests.swift"]
autonomous: true
requirements: ["PLAY-02","PLAY-03"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Seek uses confirmed source time, preserves paused intent, clamps valid ranges and cannot bypass an intervening pause or headphone-loss latch."
    - "Seek failure reconciles actual confirmed position, stays paused and retains the original technical error; unknown duration never supplies a fabricated slider range."
    - "Known long-recording landmarks across required codecs meet the declared 100 ms seek target using actual confirmed decoder positions."
  artifacts:
    - path: "MusicPlayer/Audio/SourceTimeline.swift"
      provides: "Integer source-frame to portable time accounting"
    - path: "MusicPlayerTests/SeekTests.swift"
      provides: "Absolute seek, generation and failure assertions"
  key_links:
    - from: "MusicPlayer/Audio/SourceTimeline.swift"
      to: "MusicPlayer/Audio/PCMProducer.swift"
      via: "Seek origin plus actually rendered source progress"
---

# 01-14: Confirmed source-time seeking for long recordings

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Seek through the real decoder and reconcile confirmed source position.
Purpose: Long-recording seeks remain accurate, truthful and subject to current playback permission.
Output: MusicPlayer/Audio/SourceTimeline.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayer/Audio/PCMProducer.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW SourceTimeline owns source sample rate, confirmed source frame origin and rendered progress; integer milliseconds are used only across persistence/UI boundaries.

## Dependencies and scope

Needs Plan 13's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 14 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-14-T1: Seek through the real decoder and reconcile confirmed source position</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Audio/SourceTimeline.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayer/Audio/PCMProducer.swift, MusicPlayerTests/SeekTests.swift, MusicPlayerTests/LongRecordingTests.swift</files>
  <behavior>Paused seek never starts; seek while playing resumes only still-current intent; repeated seeks supersede older results; failure/timeout restores actual confirmed cursor paused; negative/nonfinite/out-of-duration input cannot enter decoder.</behavior>
  <action>Implement source-frame seek conversion and confirmation on the shared path. Clamp finite requested seconds to known duration, reject unknown/nonfinite input with a structured error, increment operation generation, stop/reset scheduled audio, reposition decoder, refill bounded buffers and conditionally restart only after current permission recheck. Preserve paused state across successful seeks. SourceTimeline uses seek origin plus heard/rendered progress converted across source/output rates; decoded/scheduled progress does not advance display. On failure query the actual confirmed cursor if possible, retain the last reliable confirmation otherwise, pause and record requested versus confirmed positions without mislabeling the requested time as success. Timeout/cancel invalidates callbacks and frees pending buffers. Validate the existing known landmarks across every required codec, especially VBR, Opus preroll and MP4 edits.</action>
  <acceptance_criteria>Seek/long-recording tests prove confirmed timing within the declared 100 ms landmark target and no start after intervening Pause/loss; failed seeks show true paused position.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite SeekTests --suite LongRecordingTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Seek/long-recording tests prove confirmed timing within the declared 100 ms landmark target and no start after intervening Pause/loss; failed seeks show true paused position.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Absolute seek input and delayed decoder completion → shared source time and playback permission.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-14-1 | Information disclosure | Seek completion after loss | high | mitigate | Same generation/permission start check; adverse ordering tests. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Seek uses confirmed source time, preserves paused intent, clamps valid ranges and cannot bypass an intervening pause or headphone-loss latch.
Seek failure reconciles actual confirmed position, stays paused and retains the original technical error; unknown duration never supplies a fabricated slider range.
Known long-recording landmarks across required codecs meet the declared 100 ms seek target using actual confirmed decoder positions.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-14-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
