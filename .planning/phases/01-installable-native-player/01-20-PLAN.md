---
phase: 01-installable-native-player
plan: "20"
type: execute
wave: 20
depends_on: ["01-19"]
files_modified: ["MusicPlayerUITests/PhaseAcceptanceTests.swift","scripts/native_check.py",".github/workflows/ci.yml","docs/device-validation.md","docs/codec-matrix.md"]
autonomous: false
requirements: ["APP-01","APP-02","APP-03","BUILD-01","PLAY-01","PLAY-02","PLAY-03","PLAY-04","PLAY-05","PLAY-06","PLAY-07","LIB-01","SKIN-01"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The final source SHA passes a clean native build and nonzero unit/UI suites and is the SHA downloaded, sideloaded and exercised on the phone."
    - "All 44 UI considerations have measured/observed acceptance evidence, with eight distinct targets at 375 pt and required large-text/VoiceOver states."
    - "Per D-23, real wired unplug, Bluetooth loss/reconnect, interleaved resume, locked next-file opening and controls pass with no unexpected speaker audio until explicit Play."
    - "All required codecs, long seeks, USB persistence and silent relaunch work on the declared environments; absent evidence remains outstanding."
    - "No phase requirement, flagged probe item or high/critical threat is silently marked passed by planning or source review."
  artifacts:
    - path: "MusicPlayerUITests/PhaseAcceptanceTests.swift"
      provides: "Integrated native navigation/playback/error accessibility paths"
    - path: "scripts/native_check.py"
      provides: "Final count/SHA/result manifest validation"
    - path: "docs/device-validation.md"
      provides: "Exact-source physical and visual results"
    - path: "docs/codec-matrix.md"
      provides: "Final per-format/environment results"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "download_builds.py"
      via: "Single source manifest/checksum/artifact identity for final tested build"
    - from: "docs/device-validation.md"
      to: "docs/codec-matrix.md"
      via: "Same source SHA and named environments across native/physical evidence"
---

# 01-20: Final native, accessibility and physical-device acceptance

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Produce the concrete final artifact and verify every physical and rendered completion condition.
Purpose: Prevent simulator/source success from being mistaken for a verified installable player.
Output: MusicPlayerUITests/PhaseAcceptanceTests.swift, scripts/native_check.py, docs/device-validation.md, docs/codec-matrix.md.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW PhaseAcceptanceTests uses actual production routes with generated/manifest fixtures, not fake later-feature controls. Extend native-evidence.json schema with sourceSHA, dirtyState, toolchain, runnerImage, deploymentTarget, testDestination, executedTestCountByClass, failedTestCount, resultBundles, releaseBuild, artifactSHA256 and matrixResultPath. Check-evidence resolves HEAD via git rev-parse and rejects stale/missing data; physical evidence is separate and never inferred from this schema. Existing device-validation.md records user-observed outcomes and actual output routes, with failed and pending rows preserved.

## Dependencies and scope

Needs the preceding plan's implemented capability; creates the artifacts below. Sequential wave 20 follows shared-file ownership and the project's sequential execution setting.

<tasks>
<task type="auto" tdd="true">
  <name>01-20-T1: Build and review the complete final app on the pinned native route</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayerUITests/PhaseAcceptanceTests.swift, scripts/native_check.py, .github/workflows/ci.yml, docs/device-validation.md, docs/codec-matrix.md</files>
  <behavior>Integrated tests traverse all eight destinations, loaded/empty/preparing/error/seek/restore states and inspect full diagnostics; test filtering with missing classes fails; downloaded final artifact SHA matches actual successful source/test results.</behavior>
  <action>Complete the cross-feature native acceptance suite for every requirement and every lifted UI row. Run clean build-for-testing/full suite and Release build through the pinned scripts; verify installed resource/model/icon membership, architecture, linked frameworks, deployment target and conditional codec link map. native_check records actual nonzero counts per requested class and current source SHA, rejects stale/dirty evidence for final delivery, creates unique result bundles, and produces build/native-evidence.json. Workflow explicitly uploads that file, build manifest, checksum, IPA, codec report and xcresult bundles in the named successful artifact; failed test results remain available separately without presenting a successful IPA. Confirm downloader retrieves/matches these exact files. Exercise 375-by-667 and target dimensions in portrait/landscape, maximum accessibility type, VoiceOver labels/order/focus/seek adjustments, Reduce Motion, contrast, Unicode/duplicate filenames, no metadata/unknown duration, simultaneous warning/compact/navigation, long cause chains and every selected destination. Review all source changes and fix failures in their owning implementation tasks before preparing the final commit. The native check may require user-triggered remote runs; follow the no-push rule at each real execution need, never substitute Windows tests.</action>
  <acceptance_criteria>The final reviewed source has clean native unit/UI/Release results, nonzero requested counts, complete format report and validated downloadable artifact. Final phone acceptance remains pending until Task 2.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --full --release</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
    <human-check>Record actual rendered geometry/contrast and VoiceOver findings; screenshots cannot prove silence or locked-file access.</human-check>
  </verify>
  <done>The final reviewed source has clean native unit/UI/Release results, nonzero requested counts, complete format report and validated downloadable artifact. Final phone acceptance remains pending until Task 2.</done>
</task>

<task type="checkpoint:human-action">
  <name>01-20-T2: Install and verify the final source on the real iPhone</name>
  <read_first>Actual final native evidence/IPA checksum and download result; all pending physical rows in 01-VALIDATION.md and docs/device-validation.md; SKELETON.md probe assumptions and prohibitions.</read_first>
  <files>docs/device-validation.md, docs/codec-matrix.md</files>
  <action>Prepare the exact successful IPA and numbered acceptance procedure before invoking the user's notification script. Only when needed, the user pushes the reviewed final commit; the agent checks CI, downloads and validates it, and the user signs/installs through the existing sideloader. Record exact source SHA, artifact hash, iPhone model/OS, sideloader and output device. Verify USB copy with app closed/open, foreground mid-copy, cable loss, retry overwrite, same-name replacement and post-disconnect playback using the accepted readiness workflow from Plan 02; unresolved intent-completeness remains a failure/pending item. Play and seek every required codec/container and long landmarks; confirm memory/seek target measurements. While playing and while locked, unplug wired output, lose Bluetooth, reconnect, and interleave interruptions/shouldResume timing, seek/preparation completions and automatic next-file transitions. Include loss immediately before a previously unopened B starts after A ends, explicit remote Next while inhibited, direct Files selection while inhibited, and explicit app/system Play afterward. Reconnection/selection/Next/seek must leave silence until that Play; capture any speaker transient as failed acceptance. Calls resume only with earlier active intent, OS permission and no later pause/loss. Exercise all system transport/position controls, verify fresh next-file and DB access while locked after first unlock, and terminate/relaunch at a known position to confirm silent restoration. Verify large-text/small-phone/VoiceOver usability, full copied diagnostics with optional logging off, and retained errors after successful playback. Obtain minimum-OS runtime evidence or keep it outstanding. Fix failures, rebuild and retest affected checks on the new final SHA; never transfer passing evidence from a different build without explicit traceability. Only then can the phase be complete.</action>
  <acceptance_criteria>All physical rows have real results for the final source, no critical headphone-loss case is untested, no required codec/readiness/runtime claim is unsupported, and any failed/high threat remains open until fixed. Probe assumptions/prohibitions are reviewed explicitly rather than inferred passed.</acceptance_criteria>
  <verify>
    <automated>python scripts/native_check.py --check-evidence build/native-evidence.json --expected-sha HEAD</automated>
    <fails_when>Nonzero exit, missing/mismatched final SHA/artifact/native report, zero tests or failed required native case. Physical completion additionally requires the actual observations below.</fails_when>
    <human-check>Every phone, USB, output-route, minimum-OS and accessibility requirement above must be observed and recorded. Missing evidence is pending, never success.</human-check>
  </verify>
  <done>All physical rows have real results for the final source, no critical headphone-loss case is untested, no required codec/readiness/runtime claim is unsupported, and any failed/high threat remains open until fixed. Probe assumptions/prohibitions are reviewed explicitly rather than inferred passed.</done>
  <verification>Run the preceding automated evidence check and inspect the required human observations; missing or failed observations remain pending.</verification>
  <instructions>User installs the prepared artifact and performs the real device interactions. The agent retains/fixes failures and collects the resulting observations; user-only pushes remain required for new remote source builds.</instructions>
  <resume-signal>Provide the recorded result and source SHA; a failed or missing result remains open.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust boundaries

Verified source/artifact → installed device; real physical route and lock state → observable audio/UI.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-20-1 | Information disclosure | Unwanted speaker playback | high | mitigate | Mandatory physical event permutations on final build; any transient or untested critical case prevents completion. |
| T-01-20-2 | Tampering | Build/result identity | high | mitigate | Same source SHA/checksum across native result, download, sideload and physical report. |
| T-01-20-3 | Denial of service | Long file and locked progression | high | mitigate | Real bounded-memory/seek and unopened locked-file checks with documented failures. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
The final source SHA passes a clean native build and nonzero unit/UI suites and is the SHA downloaded, sideloaded and exercised on the phone. All 44 UI considerations have measured/observed acceptance evidence, with eight distinct targets at 375 pt and required large-text/VoiceOver states. Per D-23, real wired unplug, Bluetooth loss/reconnect, interleaved resume, locked next-file opening and controls pass with no unexpected speaker audio until explicit Play. All required codecs, long seeks, USB persistence and silent relaunch work on the declared environments; absent evidence remains outstanding. No phase requirement, flagged probe item or high/critical threat is silently marked passed by planning or source review.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-20-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
