---
phase: 01-installable-native-player
plan: "13"
type: execute
wave: 13
depends_on: ["01-12"]
files_modified: ["MusicPlayer/Domain/PlaybackQueue.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayer/Feature/FilesView.swift","MusicPlayerTests/PlaybackQueueTests.swift","MusicPlayerTests/PlaybackRaceTests.swift","MusicPlayer/Persistence/ResumeSnapshot.swift","MusicPlayer/Persistence/LibraryStore.swift","MusicPlayerTests/RestorationTests.swift","MusicPlayerUITests/RestorationUITests.swift"]
autonomous: true
requirements: ["PLAY-02","PLAY-07","APP-03"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-16/D-17, selecting a ready Files row starts at zero while remaining in Files and freezes that displayed sequence as the queue."
    - "Per D-18, Previous immediately selects the prior song at zero; first Previous and final Next are unavailable, with no wraparound."
    - "Per D-20, automatic failure traversal visits each remaining candidate at most once, records failures and stops when exhausted."
    - "Per D-19, relaunch restores the prior loaded track, confirmed position, queue and destination while paused; missing media gives a retained error."
    - "Direct-selection failure pauses and retains the previous loaded playable item, never skips to an unrelated track; headphone-loss inhibition survives all selection/restoration paths."
  artifacts:
    - path: "MusicPlayer/Domain/PlaybackQueue.swift"
      provides: "Frozen ordered IDs and bounded forward traversal"
    - path: "MusicPlayer/Persistence/ResumeSnapshot.swift"
      provides: "Versioned paused-restoration values"
    - path: "MusicPlayerTests/PlaybackQueueTests.swift"
      provides: "Boundary and failure behavior"
    - path: "MusicPlayerTests/RestorationTests.swift"
      provides: "Disk round-trip and no-start assertions"
  key_links:
    - from: "MusicPlayer/Feature/FilesView.swift"
      to: "MusicPlayer/Domain/PlaybackQueue.swift"
      via: "Selection sends exact displayed IDs once"
    - from: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      to: "MusicPlayer/Persistence/ResumeSnapshot.swift"
      via: "Save confirmed time/queue and restore without start intent"
---

# 01-13: Displayed-order playback queue and silent restoration

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Turn individual playback into the approved queue and relaunch behavior.
Purpose: Tracks advance predictably and the app reopens at the user's last confirmed position without surprise audio.
Output: MusicPlayer/Domain/PlaybackQueue.swift, MusicPlayer/Persistence/ResumeSnapshot.swift, MusicPlayerTests/PlaybackQueueTests.swift, MusicPlayerTests/RestorationTests.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW PlaybackQueue {orderedTrackIDs, currentIndex, traversalGeneration, failedTrackIDSet} takes the already displayed sequence; no filesystem query inside queue. Previous/Next preserve current playing/paused intent subject to PlaybackPolicy; file selection requests playing except when explicit-play inhibition applies. NEW ResumeSnapshot {schemaVersion, trackID, confirmedPositionMilliseconds, queueTrackIDs, selectedDestination}; store only confirmed position and persist on meaningful changes plus bounded interval. Existing LibraryStore owns storage, current-root resolution and error paths; new value type is not a forwarding persistence wrapper.

## Dependencies and scope

Needs the preceding plan's implemented capability; creates the artifacts below. Sequential wave 13 follows shared-file ownership and the project's sequential execution setting.

<tasks>
<task type="auto" tdd="true">
  <name>01-13-T1: Navigate a frozen queue and skip failed automatic candidates once</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Domain/PlaybackQueue.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayer/Feature/FilesView.swift, MusicPlayerTests/PlaybackQueueTests.swift, MusicPlayerTests/PlaybackRaceTests.swift</files>
  <behavior>Empty/one/many queue boundaries; previous well into a song still selects prior at zero; direct failure retains prior paused; A good → B missing → C corrupt → D good skips once; all-failed stops; scan insertion does not mutate queue; loss near EOF never starts next.</behavior>
  <action>Implement D-16 through D-20 against the displayed ID snapshot already supplied by Files. On direct selection, stage target metadata and generation; stop/pause prior audio immediately, commit loaded item only after preparation succeeds, otherwise retain prior loaded details/confirmed position paused and record target failure separately. On success start from zero when permitted and remain in Files. Previous selects prior at zero immediately, Next selects following at zero, both preserving playing versus paused intent and always honoring loss inhibition. Disable first Previous/final Next. Audible EOF uses played-back completion from the producer; traverse each remaining candidate at most once per forward attempt, recording each failure with skip/stopped recovery. If loss or Pause occurs during skip preparation, keep the chosen result paused rather than continuing audible progression. Exhaustion stops without cycling. At final natural end retain final duration/status; explicit Play resets final track to zero. Ready discovery updates the visible library only, not the active queue.</action>
  <acceptance_criteria>Queue/race tests assert exact IDs, start positions, adjacent boundaries, bounded failure visits and zero forbidden starts; Files stays selected.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite PlaybackQueueTests --suite PlaybackRaceTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Queue/race tests assert exact IDs, start positions, adjacent boundaries, bounded failure visits and zero forbidden starts; Files stays selected.</done>
</task>

<task type="auto" tdd="true">
  <name>01-13-T2: Restore real persisted position and page without playback</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Persistence/ResumeSnapshot.swift, MusicPlayer/Persistence/LibraryStore.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/RestorationTests.swift, MusicPlayerUITests/RestorationUITests.swift</files>
  <behavior>Save/reopen the actual store preserves ID/queue/page/confirmed time with no play/session-start; position clamps to current duration; missing/replaced track reports a technical error; failed save retains current playback and usable error.</behavior>
  <action>Implement D-19 and D-06 with versioned value snapshots saved off main at transitions and a named throttled interval. Persist confirmed source time, never provisional seek drag or decoded-ahead position. On app launch resolve the current Documents root, track UUID and revision, validate available queue IDs and clamp stored position to known duration. Restore selection and decoder position through the same generation-aware prepare/seek path with paused intent; neither scene activation nor successful restore clears requiresExplicitPlay or activates sound. Restore page independently, defaulting unknown values safely to Files with a diagnostic. Missing restored media retains its last filename/location for the exact restoration message; preserve valid other files and make Go to Files usable. Handle a save failure visibly without corrupting last good disk state.</action>
  <acceptance_criteria>Disk-backed unit/UI tests terminate and relaunch, then prove the correct page/track/time is restored with zero engine start and paused system intent, including missing/replaced media and failed persistence.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite RestorationTests --ui-suite RestorationUITests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>Disk-backed unit/UI tests terminate and relaunch, then prove the correct page/track/time is restored with zero engine start and paused system intent, including missing/replaced media and failed persistence.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Mutable library state → frozen playback queue; persisted snapshot → reopened app/media cursor.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-13-1 | Tampering | Stale file/queue identity | high | mitigate | Validate revision and stable ID; preserve loaded item on direct failure and never attach replacement by basename. |
| T-01-13-2 | Information disclosure | Restoration/autoprogression start | high | mitigate | Use shared paused intent/generation and only explicit Play clears loss. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-16/D-17, selecting a ready Files row starts at zero while remaining in Files and freezes that displayed sequence as the queue. Per D-18, Previous immediately selects the prior song at zero; first Previous and final Next are unavailable, with no wraparound. Per D-20, automatic failure traversal visits each remaining candidate at most once, records failures and stops when exhausted. Per D-19, relaunch restores the prior loaded track, confirmed position, queue and destination while paused; missing media gives a retained error. Direct-selection failure pauses and retains the previous loaded playable item, never skips to an unrelated track; headphone-loss inhibition survives all selection/restoration paths.
</success_criteria>

## Flagged assumptions

EDGE PLAY-07/unclassified remains unresolved classifier accounting and is tied to real disk/relaunch/UI no-start tests. No existing test result is claimed.

<output>
Create .planning/phases/01-installable-native-player/01-13-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
