---
phase: 01-installable-native-player
plan: "05"
type: execute
wave: 5
depends_on: ["01-04"]
files_modified: ["MusicPlayer/Diagnostic/DiagnosticRecord.swift","MusicPlayer/Diagnostic/DiagnosticStore.swift","MusicPlayer/Library/TransferDiscovery.swift","MusicPlayer/Feature/FilesView.swift","MusicPlayerTests/DiagnosticStoreTests.swift"]
autonomous: true
requirements: ["LIB-01","APP-02"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-25/D-26/D-27, discovery failures retain operation, file, original message/domain/code, cause chain and recovery action in bounded persistent history."
    - "Diagnostic history remains inspectable after relaunch with optional logging disabled; successful scans and playback do not delete retained records."
    - "Failed history writes preserve in-memory inspectability without recursive logging or main/render-thread file I/O."
  artifacts:
    - path: "MusicPlayer/Library/TransferDiscovery.swift"
      provides: "Foreground reconciliation, readiness and revision checks"
    - path: "MusicPlayer/Diagnostic/DiagnosticRecord.swift"
      provides: "Structured errors with bounded causes"
    - path: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      provides: "Bounded asynchronous persistent diagnostic history"
  key_links:
    - from: "MusicPlayer/Library/TransferDiscovery.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      via: "Retain original discovery failures independently of optional logging"
    - from: "MusicPlayer/Feature/FilesView.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      via: "Selectable warnings expose retained records"
---

# 01-05: Persist inspectable discovery failures across relaunch

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Retain complete discovery errors across relaunch without optional file logging.
Purpose: The technical user can inspect prior failures after a later scan or playback succeeds.
Output: MusicPlayer/Diagnostic/DiagnosticRecord.swift, MusicPlayer/Diagnostic/DiagnosticStore.swift, MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Feature/FilesView.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW DiagnosticRecord {id, time, operation, relativeLocator, originalMessage, domain, code, underlyingCauses, recoveryAction}; NEW DiagnosticStore actor initially retains a bounded recent history through Application Support independent of optional logging. TransferDiscovery publishes these records and Files exposes selectable warning details.

## Dependencies and scope

Needs Plan 04's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 5 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-05-T1: Keep discovery failures inspectable across relaunch</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Diagnostic/DiagnosticRecord.swift, MusicPlayer/Diagnostic/DiagnosticStore.swift, MusicPlayer/Library/TransferDiscovery.swift, MusicPlayer/Feature/FilesView.swift, MusicPlayerTests/DiagnosticStoreTests.swift</files>
  <behavior>Nested NSError and decoder causes preserve original message/domain/code; cyclic/deep causes are bounded; disabled optional logging still retains inspectable recent records after store reload; repeated errors do not grow without bound.</behavior>
  <action>Introduce the shared error record and actor store per D-25/D-26/D-27, replacing temporary tracer error storage without losing original causes. Preserve operation/file/recovery action and bounded recursive underlying causes; store up to the configured recent-record limit (default 200), coalesce repeated identical failures by stable identity/count, and persist asynchronously outside audio/main threads. Describe retention as the latest configured count, not permanent history. Keep in-memory diagnostics usable after persistence failure, recording that failure without recursive logging. Files renders a warning with expandable selectable record text until the dedicated shared detail screen arrives; successful scan/playback does not delete records. Optional verbose logging is injectable and disabled by default; do not expose a UI switch before it exists.</action>
  <acceptance_criteria>DiagnosticStoreTests prove bounded persisted history survives relaunch with optional logging disabled and failed history writes preserve current inspectability.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite DiagnosticStoreTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>DiagnosticStoreTests prove bounded persisted history survives relaunch with optional logging disabled and failed history writes preserve current inspectability.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

File/system cause chains → retained Application Support history and selectable Files warnings.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-05-1 | Denial of service | Cause cycles and repeated history failures | high | mitigate | Bound cause depth, coalesce repeated failures, cap retention and avoid recursive logging with worker I/O. |
| T-01-05-2 | Information disclosure | Diagnostic history | medium | mitigate | Keep history outside Documents in Application Support and preserve inspectability after write failure. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-25/D-26/D-27, discovery failures retain operation, file, original message/domain/code, cause chain and recovery action in bounded persistent history.
Diagnostic history remains inspectable after relaunch with optional logging disabled; successful scans and playback do not delete retained records.
Failed history writes preserve in-memory inspectability without recursive logging or main/render-thread file I/O.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-05-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
