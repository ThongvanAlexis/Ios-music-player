---
phase: 01-installable-native-player
plan: "08"
type: execute
wave: 8
depends_on: ["01-07"]
files_modified: ["MusicPlayer/Resources/Assets.xcassets/Contents.json","MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json","MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png","MusicPlayer.xcodeproj/project.pbxproj","MusicPlayerUITests/SkinLayoutTests.swift"]
autonomous: true
requirements: ["APP-02","APP-03","SKIN-01"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "APP-01 adjacency edge: eight neighboring navigation targets do not overlap at 375 pt or in measured supported landscape safe widths."
    - "The actual installed app bundle contains the project-owned native app icon."
    - "Rendered skin controls meet the required contrast, scrolling and accessibility targets at measured supported phone sizes; missing environments remain pending."
  artifacts:
    - path: "MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png"
      provides: "Project-owned installed app icon"
    - path: "MusicPlayerUITests/SkinLayoutTests.swift"
      provides: "Measured native geometry and appearance evidence"
  key_links:
    - from: "MusicPlayer.xcodeproj/project.pbxproj"
      to: "MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json"
      via: "Compile and select the actual app icon catalog"
---

# 01-08: Installed app icon and measured small-screen appearance

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Give the installed app its bundled icon and verify native-scale geometry and readability.
Purpose: The real app remains distinguishable and usable on the supported small-phone layouts.
Output: MusicPlayer/Resources/Assets.xcassets/Contents.json, MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json, MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png, MusicPlayer.xcodeproj/project.pbxproj.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW Assets.xcassets and AppIcon.appiconset contain the project-owned native icon. Existing project target selects that catalog icon; NEW SkinLayoutTests measures the existing shared skin and navigation without redefining roles or geometry.

## Dependencies and scope

Needs Plan 07's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 8 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-08-T1: Give the installed native app its bundled icon and inspect small-screen appearance</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Resources/Assets.xcassets/Contents.json, MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/Contents.json, MusicPlayer/Resources/Assets.xcassets/AppIcon.appiconset/AppIcon.png, MusicPlayer.xcodeproj/project.pbxproj, MusicPlayerUITests/SkinLayoutTests.swift</files>
  <behavior>App icon resources compile into the actual app; measured 375-by-667 and target-size screens have readable contrast, separate targets and no horizontal overflow at accessibility sizes.</behavior>
  <action>Create a project-owned app icon consistent with the understated music-player appearance, include it in the native asset catalog and configure the app target's icon name. Keep app icon resources distinct from skin roles. Add UI checks at 375-by-667 and target-phone dimensions, portrait and landscape, default and largest accessibility text settings. Measure actual frames, selected/pressed/disabled contrast (4.5:1 normal text, 3:1 meaningful controls), wrap headings and scroll content above the bottom inset. Verify Reduce Motion and increased contrast/reduced transparency preserve immediate readable feedback. Do not infer that a requested simulator size exists; use available small-phone runtime or report the missing environment and retain the physical measurement requirement.</action>
  <acceptance_criteria>The actual app bundle contains a valid icon and SkinLayoutTests report measured geometry and image/contrast review artifacts; unavailable device sizes remain explicit pending checks.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite SkinTests --ui-suite SkinLayoutTests --release</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
    <human-check>Inspect native-scale composite icons and screenshots for readable distinction; source dimensions alone do not prove usable hit areas.</human-check>
  </verify>
  <done>The actual app bundle contains a valid icon and SkinLayoutTests report measured geometry and image/contrast review artifacts; unavailable device sizes remain explicit pending checks.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Compiled icon assets and rendered shared controls → installed app and native user interaction.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-08-1 | Denial of service | Small-screen controls and long text | medium | mitigate | Measure separate hit areas and rendered contrast; require wrapping, scrolling and accessibility settings without covered actions. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
APP-01 adjacency edge: eight neighboring navigation targets do not overlap at 375 pt or in measured supported landscape safe widths.
The actual installed app bundle contains the project-owned native app icon.
Rendered skin controls meet the required contrast, scrolling and accessibility targets at measured supported phone sizes; missing environments remain pending.
</success_criteria>

## Flagged assumptions

EDGE SKIN-01/unclassified remains unresolved as a classifier assumption tied to SkinTests, rendered contrast and physical small-phone inspection; the explicit UI criteria are planned, not passed.

<output>
Create .planning/phases/01-installable-native-player/01-08-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
