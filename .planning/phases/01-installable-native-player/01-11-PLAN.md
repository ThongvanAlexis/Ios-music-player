---
phase: 01-installable-native-player
plan: "11"
type: execute
wave: 11
depends_on: ["01-10"]
files_modified: ["NativeCodec/sources.lock.json","scripts/build_native_codecs.py","docs/dependency-review.md","NativeCodec/module.modulemap","scripts/native_check.py"]
autonomous: true
requirements: ["PLAY-01","PLAY-03"]
estimate:
  tokens: 15333
  raw_tokens: 15333
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Canonical Xiph pins include the reviewed Vorbis seek and opusfile allocation/picture fixes; every included build module/transitive source is reviewed before execution."
    - "Only measured Apple gaps adopt native sources; passing Apple formats do not introduce extra dependencies."
    - "A clean native runner rebuilds the required device/simulator slices and consumes reviewed platform-matching settings; absent runtime evidence remains unresolved."
  artifacts:
    - path: "NativeCodec/sources.lock.json"
      provides: "Conditional exact canonical source/digest inventory"
    - path: "scripts/build_native_codecs.py"
      provides: "Reviewed pinned static library device/simulator builds"
  key_links:
    - from: "scripts/build_native_codecs.py"
      to: "NativeCodec/sources.lock.json"
      via: "Digest-checked reviewed source only"
    - from: "scripts/native_check.py"
      to: "scripts/build_native_codecs.py"
      via: "Conditionally rebuild adopted source slices and consume generated xcconfig on clean builds"
---

# 01-11: Reviewed source builds for measured decoder gaps

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Build only the reviewed native source slices needed by the measured Apple format gaps.
Purpose: Native tests and Release builds can recreate required decoder dependencies from exact reviewed origins.
Output: NativeCodec/sources.lock.json, scripts/build_native_codecs.py, docs/dependency-review.md, NativeCodec/module.modulemap, scripts/native_check.py.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

Conditional NEW native source pins: ogg 06a5e0262cdc28aa4ae6797627a783b5010440f0; vorbis/vorbisfile 1b75110b5a2754ba1931d82dd83cb822b266a21d; opus official 1.6.1 archive SHA256 6ffcb593207be92584df15b32466ed64bbec99109f007c82205f0194572411a1; opusfile 6dfd29e7adb87f2e193575fc3fa88cbf1a0b27df. Canonical origins are gitlab.xiph.org/xiph/{ogg,vorbis,opusfile} and opus-codec.org official archive. Build outputs live in ignored build/NativeCodec, not source-controlled binaries.

## Dependencies and scope

Needs Plan 10's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 11 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-11-T1: Build only reviewed native sources needed by failed Apple cells</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>NativeCodec/sources.lock.json, scripts/build_native_codecs.py, docs/dependency-review.md, NativeCodec/module.modulemap, scripts/native_check.py</files>
  <behavior>Wrong source digest, missing fix, unexpected build step/target or wrong deployment slice fails before linkage; Apple passing formats do not cause extra decoder adoption.</behavior>
  <action>Read the actual Plan 10 matrix first. If no required Xiph gap exists, record that measured result and leave native sources unadopted; do not manufacture dependency work. Otherwise retrieve only needed canonical pins listed above, record archive digests and available signed-origin/integrity information, and preserve licenses. Recheck the exact Vorbis page-seek fix and opusfile allocation/picture changes. Complete recursive CMake/generated-header/transitive review before configuring or compiling; no upstream CI/autogen/example scripts are trusted instructions. Assert CMake 4.4.3 and Apple toolchain from build_config.json. Build static Ogg first, Vorbis and Opus as needed next, opusfile last, separately for device arm64 and simulator arm64, deployment 17.0. Explicitly list library sources/targets, configure BUILD_SHARED_LIBS=OFF, OP_DISABLE_HTTP=ON, OP_DISABLE_EXAMPLES=ON, OP_DISABLE_DOCS=ON, OPUS_BUILD_PROGRAMS=OFF, OPUS_BUILD_TESTING=OFF, OPUS_DRED=OFF and OPUS_OSCE=OFF; float decoding stays enabled. Confirm supported option names in pinned build source and fail on unused/unknown security-relevant options. Produce matching local XCFrameworks with xcodebuild -create-xcframework or explicitly linked matching static slices; keep network URL libraries, OpenSSL, encoders and optional neural components out of app link maps. The native registry checker does not cover this ecosystem; document source review without fabricated package verdicts. Build script uses configured timeouts and argument arrays, a reviewed cache and checksum validation. Create the module map for the app-owned bridge header; preserve complete dependency license texts/notices in docs/dependency-review.md or a reviewed source archive referenced there. Configure initial project search/link settings through the script-produced configuration consumed by the native runner before Xcode build. Wire native_check.py to call build_native_codecs.py before every clean native build when sources.lock.json records an adopted measured gap. The build script uses upstream reviewed explicit library targets, generates a local xcconfig with platform-matching search/link flags, builds DecoderBridge.c when the adapter task has created it, and passes the generated xcconfig into every test/Release xcodebuild invocation. A clean CI checkout must recreate all native libraries/bridge outputs; it cannot rely on a prior local build cache. No extra app-owned CMakeLists.txt is needed.</action>
  <acceptance_criteria>Either measured Apple completeness is recorded without adoption, or each required native device/simulator build has exact reviewed origin/digest, correct architecture/minimum OS and an inspected link map. No unresolved high source threat is accepted. The actual native runner invokes the conditional build and consumes its xcconfig on a clean checkout.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/build_native_codecs.py --verify-required-gaps docs/codec-matrix.md</automated>
    <fails_when>Nonzero exit; missing runtime gap evidence, unreviewed source/build input, wrong digest/toolchain/platform slice or unexpected linked target. Apple-only disposition must reference the complete nonzero runtime matrix.</fails_when>
  </verify>
  <done>Either measured Apple completeness is recorded without adoption, or each required native device/simulator build has exact reviewed origin/digest, correct architecture/minimum OS and an inspected link map. No unresolved high source threat is accepted. The actual native runner invokes the conditional build and consumes its xcconfig on a clean checkout.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Canonical native source → reviewed build → native runner link configuration.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-11-1 | Tampering | Native codec sources/build graph | high | mitigate | Exact canonical revisions/digests and full recursive build/fix review before execute. |
| T-01-11-3 | Information disclosure | Unexpected native dependencies | high | mitigate | Disable HTTP/url/TLS path and inspect actual symbols/link map. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Canonical Xiph pins include the reviewed Vorbis seek and opusfile allocation/picture fixes; every included build module/transitive source is reviewed before execution.
Only measured Apple gaps adopt native sources; passing Apple formats do not introduce extra dependencies.
A clean native runner rebuilds the required device/simulator slices and consumes reviewed platform-matching settings; absent runtime evidence remains unresolved.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-11-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
