---
phase: 01-installable-native-player
plan: "04"
type: execute
wave: 4
depends_on: ["01-03"]
files_modified: ["MusicPlayer/Feature/FilesView.swift","MusicPlayer/Feature/FileDetailsView.swift","MusicPlayer/App/AppCopy.swift","MusicPlayerUITests/FilesViewTests.swift","MusicPlayerTests/FilesOrderTests.swift"]
autonomous: true
requirements: ["LIB-01","APP-02","PLAY-01"]
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-10, filenames lead rows, optional metadata is omitted when absent, duplicate basenames show relative paths, and File Details exposes full selectable Unicode text."
    - "APP-01 ordering edge: explicit filename comparison plus stable UUID tie-break creates one sequence shared by Files and queue creation."
    - "APP-02 encoding/empty edges: generated labels stay English, original metadata/errors preserve Unicode verbatim, and absent values use approved empty copy."
    - "UI empty/files: Zero ready tracks show the Files empty copy from Copywriting Contract; pending transfers remain distinguishable."
    - "UI loading/files: Initial and refresh scans use their documented copy, preserve existing playable rows and never show invented progress."
    - "UI error/files: Discovery and row failures remain inspectable while unaffected ready rows remain available."
    - "UI populated/files: Ready rows show filename first, available metadata beneath and a distinct current-track indicator in explicit stable order."
    - "UI partial/files: Pending rows cannot start playback; missing metadata lines are omitted; confirmed ready rows remain playable."
    - "UI overflow/files: Files scroll vertically above the compact player and navigation; rows expand for text."
    - "UI zero-one-many/files: Zero uses the documented empty state, one uses an ordinary row, and many use the same scrolling list without artificial filler."
    - "UI long-text/files: Ordinary filenames use the specified two-line middle truncation; accessibility text wraps fully and File Details exposes the full name."
    - "UI loading/file-details: Available file facts render immediately; optional metadata loading does not block dismissal or navigation."
    - "UI error/file-details: Unavailable files or metadata retain the available filename and relative location and expose the underlying error."
    - "UI overflow/file-details: File Details scrolls vertically with a reachable Close File Details control and selectable full content."
    - "UI long-text/file-details: Full Unicode filename, relative location and available metadata wrap without truncation."
  artifacts:
    - path: "MusicPlayer/Feature/FileDetailsView.swift"
      provides: "Read-only complete file information"
    - path: "MusicPlayer/App/AppCopy.swift"
      provides: "Central exact UI copy"
  key_links:
    - from: "MusicPlayer/Feature/FilesView.swift"
      to: "MusicPlayer/Feature/FileDetailsView.swift"
      via: "File Details context-menu route"
---

# 01-04: Ready Files rows and complete read-only file details

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Show ready and pending files in explicit order with full selectable file details.
Purpose: The user can identify and select completed media while incomplete entries remain distinguishable.
Output: MusicPlayer/Feature/FilesView.swift, MusicPlayer/Feature/FileDetailsView.swift, MusicPlayer/App/AppCopy.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

Existing AppCopy supplies every literal/template from the approved Copywriting Contract, including loading, skip counts, later-page copy, diagnostics and dismissals; tests inspect rendered behavior, not mirrored copies. NEW FileDetailsView exposes full read-only filename, relative location, metadata and original error. FilesView consumes the preceding discovery snapshots and emits the exact displayed UUID sequence on selection.

## Dependencies and scope

Needs Plan 03's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 4 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-04-T1: Expose ready and pending rows with complete read-only file details</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Feature/FilesView.swift, MusicPlayer/Feature/FileDetailsView.swift, MusicPlayer/App/AppCopy.swift, MusicPlayerUITests/FilesViewTests.swift, MusicPlayerTests/FilesOrderTests.swift</files>
  <behavior>Zero/one/many ready rows, pending transfers, missing metadata, Unicode, duplicate names and scan errors follow the approved copy; filename/UUID ordering agrees with the sequence emitted on selection; full file text is accessible.</behavior>
  <action>Complete and reuse the exact Copywriting Contract literals/templates already centralized in AppCopy and consume them across Files and later screens. Implement flat rows at least 64 pt high, 16 pt inset, 8 pt padding, filename first and optional title/artist beneath per D-10. Use a documented case-sensitive Unicode scalar filename comparison with UUID stable tie-break, independent of filesystem order; do not normalize stored filenames or alter the actual locator. At ordinary size use two lines and middle truncation; accessibility sizes wrap fully. Duplicate basenames show relative location. Ready taps send select with a snapshot of the displayed UUID sequence and stay in Files per D-16/D-17; the coordinator's loss policy still applies. Pending rows are nonplayable with waiting copy and no invented percentage. Preserve existing rows during refresh/failure and expose current-row indicator. File Details is read-only, with full selectable filename/location/metadata/errors and reachable Close File Details. Optional metadata reads are bounded/cancellable and do not block dismissal. No picker, tree operations or later collection actions.</action>
  <acceptance_criteria>UI tests cover every Files and File Details truth above and prove navigation remains responsive during scan; one explicit order is supplied to selection. All exact copy comes from AppCopy.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite FilesOrderTests --ui-suite FilesViewTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>UI tests cover every Files and File Details truth above and prove navigation remains responsive during scan; one explicit order is supplied to selection. All exact copy comes from AppCopy.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

File metadata and error strings → native Files and File Details views.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-04-1 | Denial of service | Metadata and long text | medium | mitigate | Bound and cancel optional metadata reads, preserve responsive navigation and scroll full text. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-10, filenames lead rows, optional metadata is omitted when absent, duplicate basenames show relative paths, and File Details exposes full selectable Unicode text.
APP-01 ordering edge: explicit filename comparison plus stable UUID tie-break creates one sequence shared by Files and queue creation.
APP-02 encoding/empty edges: generated labels stay English, original metadata/errors preserve Unicode verbatim, and absent values use approved empty copy.
UI empty/files: Zero ready tracks show the Files empty copy from Copywriting Contract; pending transfers remain distinguishable.
UI loading/files: Initial and refresh scans use their documented copy, preserve existing playable rows and never show invented progress.
UI error/files: Discovery and row failures remain inspectable while unaffected ready rows remain available.
UI populated/files: Ready rows show filename first, available metadata beneath and a distinct current-track indicator in explicit stable order.
UI partial/files: Pending rows cannot start playback; missing metadata lines are omitted; confirmed ready rows remain playable.
UI overflow/files: Files scroll vertically above the compact player and navigation; rows expand for text.
UI zero-one-many/files: Zero uses the documented empty state, one uses an ordinary row, and many use the same scrolling list without artificial filler.
UI long-text/files: Ordinary filenames use the specified two-line middle truncation; accessibility text wraps fully and File Details exposes the full name.
UI loading/file-details: Available file facts render immediately; optional metadata loading does not block dismissal or navigation.
UI error/file-details: Unavailable files or metadata retain the available filename and relative location and expose the underlying error.
UI overflow/file-details: File Details scrolls vertically with a reachable Close File Details control and selectable full content.
UI long-text/file-details: Full Unicode filename, relative location and available metadata wrap without truncation.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-04-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
