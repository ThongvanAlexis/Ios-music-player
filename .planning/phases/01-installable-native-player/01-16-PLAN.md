---
phase: 01-installable-native-player
plan: "16"
type: execute
wave: 16
depends_on: ["01-15"]
files_modified: ["MusicPlayer/Audio/AudioSessionController.swift","MusicPlayer/Audio/SystemMediaController.swift","MusicPlayer/Audio/PlaybackCoordinator.swift","MusicPlayerTests/SystemMediaTests.swift","MusicPlayerTests/PlaybackRaceTests.swift"]
autonomous: true
requirements: ["PLAY-04","PLAY-05","PLAY-06"]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Per D-21 through D-24, real wired/Bluetooth route loss latches paused and reconnect/OS resume cannot clear it; physical locked next-file cases are mandatory."
    - "Lock-screen and Control Center play/pause/previous/next/seek use the same queue, boundaries, errors and explicit-Play policy as app controls."
    - "Media-service reset rebuilds graph objects without restoring forbidden playback intent or duplicating system handlers."
  artifacts:
    - path: "MusicPlayer/Audio/AudioSessionController.swift"
      provides: "Native audio session notifications and route facts"
    - path: "MusicPlayer/Audio/SystemMediaController.swift"
      provides: "One registration of shared remote commands"
    - path: "MusicPlayerTests/SystemMediaTests.swift"
      provides: "System/app intent equivalence"
  key_links:
    - from: "MusicPlayer/Audio/SystemMediaController.swift"
      to: "MusicPlayer/Audio/PlaybackCoordinator.swift"
      via: "All remote commands dispatch shared intents and return meaningful results"
    - from: "MusicPlayer/Audio/AudioSessionController.swift"
      to: "MusicPlayer/Domain/PlaybackPolicy.swift"
      via: "Interruption snapshots and wired/Bluetooth loss events"
---

# 01-16: Shared system transport and interruption handling

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Use the same playback rules for real session interruptions and system transport.
Purpose: System commands, route changes and media resets cannot bypass explicit user intent.
Output: MusicPlayer/Audio/AudioSessionController.swift, MusicPlayer/Audio/SystemMediaController.swift, MusicPlayer/Audio/PlaybackCoordinator.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW AudioSessionController owns AVAudioSession playback-category configuration, current/previous route inspection and single notification registration; it returns facts, never directly starts audio. NEW SystemMediaController owns MPRemoteCommandCenter targets and NowPlaying projection lifetime; commands map to explicitPlay/pause/previous/next/seek, with accurate success/failed/unavailable statuses. Coordinator owns activation only when a permitted request is ready.

## Dependencies and scope

Needs Plan 15's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 16 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-16-T1: Use shared commands for real interruptions and system transport</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Audio/AudioSessionController.swift, MusicPlayer/Audio/SystemMediaController.swift, MusicPlayer/Audio/PlaybackCoordinator.swift, MusicPlayerTests/SystemMediaTests.swift, MusicPlayerTests/PlaybackRaceTests.swift</files>
  <behavior>App/system Play alone clears loss; remote Next/seek obey boundaries and paused policy; late shouldResume with changed intent does nothing; route inspection recognizes wired and actual Bluetooth outputs; re-registration never duplicates actions.</behavior>
  <action>Configure AVAudioSession category playback and active background capability. Replace initial inline route observation with AudioSessionController facts while retaining the sole coordinator start authority per D-21/D-22/D-24. Inspect previous and current route plus reason to detect wired and Bluetooth headphone loss; test Bluetooth A2DP/HFP/LE route values available at baseline rather than one port enum. Use iOS17-compatible notification APIs and explicit Swift6 isolation. Register MediaPlayer play, pause, previous, next and change-position handlers once and remove them with owner lifetime; return accurate results for no track, preparing and boundary cases. Publish filename as primary title plus available metadata/artwork, duration, elapsed source time and rate after meaningful changes, never a separate clock. A media-services reset invalidates generations, rebuilds graph/session objects and preserves manual-Play inhibition. Audio session activation only accompanies authorized playback, not discovery or paused restore.</action>
  <acceptance_criteria>SystemMediaTests and race permutations show equivalent app/system commands, correct metadata/rate, handler count and no resume after loss or later pause; simulator evidence remains distinct from real output.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --suite SystemMediaTests --suite PlaybackRaceTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>SystemMediaTests and race permutations show equivalent app/system commands, correct metadata/rate, handler count and no resume after loss or later pause; simulator evidence remains distinct from real output.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

iOS notifications and remote controls → the single playback policy.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-16-1 | Information disclosure | Unexpected speaker output | high | mitigate | Preserve explicit-Play latch through all platform events and test physical delayed/interleaved next-file cases. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-21 through D-24, real wired/Bluetooth route loss latches paused and reconnect/OS resume cannot clear it; physical locked next-file cases are mandatory.
Lock-screen and Control Center play/pause/previous/next/seek use the same queue, boundaries, errors and explicit-Play policy as app controls.
Media-service reset rebuilds graph objects without restoring forbidden playback intent or duplicating system handlers.
</success_criteria>

## Flagged assumptions

EDGE PLAY-05 and PLAY-06/unclassified remain unresolved assumptions tied to physical locked/media-control/audio-route evidence. Property inspection and simulated notifications do not resolve the physical claims.

<output>
Create .planning/phases/01-installable-native-player/01-16-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
