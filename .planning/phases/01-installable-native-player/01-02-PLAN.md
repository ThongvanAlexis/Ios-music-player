---
phase: 01-installable-native-player
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: ["download_builds.py","download_builds.bat","tests/test_download_builds.py","build_config.json",".gitignore","docs/device-validation.md"]
autonomous: false
requirements: ["BUILD-01","LIB-01","APP-01"]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Per D-01 and D-02, the Windows launcher retrieves the chosen successful build into GH_builds and the user installs that source SHA through iLoader/SideStore."
    - "Per D-03, the agent never pushes, and failed downloads preserve the previous usable IPA."
    - "Per D-12 through D-15, individual USB transfers are tested on the installed app; partial-copy publication facts are recorded before general readiness behavior is implemented."
  artifacts:
    - path: "download_builds.py"
      provides: "Validated GitHub CLI artifact retrieval"
    - path: "download_builds.bat"
      provides: "Windows entry point"
    - path: "tests/test_download_builds.py"
      provides: "Selection and failure-preservation tests"
    - path: "docs/device-validation.md"
      provides: "Observed installation and USB publication behavior"
  key_links:
    - from: "download_builds.bat"
      to: "download_builds.py"
      via: "Forwards user run/SHA selection and exit status"
    - from: "download_builds.py"
      to: "build_config.json"
      via: "Shared repository/workflow/artifact identity and timeouts"
    - from: "docs/device-validation.md"
      to: "MusicPlayer/Library/TransferDiscovery.swift"
      via: "Observed completion/revalidation rules consumed by Plan 03"
  prohibitions:
    - statement: "Incomplete USB data must never be represented as confirmed complete merely because it is readable and stable."
      status: "unresolved"
---

# 01-02: Windows IPA retrieval and real USB publication probe

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Make the user's Windows delivery route usable and collect early physical evidence for transfer completion.
Purpose: Confirm installability and the external writer's behavior before declaring arbitrary audio ready.
Output: download_builds.py, download_builds.bat, tests/test_download_builds.py, docs/device-validation.md.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW download_builds.py defines select_run(run_records, requested_run_id, requested_sha), validate_artifact(staging_dir, expected_identity), download_build() and injectable subprocess runner; no HTTP or token wrapper. NEW download_builds.bat forwards arguments from its own directory. NEW tests/test_download_builds.py uses unittest and subprocess fakes. Existing build_config.json receives repository/workflow identity, retention and command timeout only once. Existing .gitignore retains /ref_pics/ exactly and ignores /GH_builds/, /build/, native build outputs and transient artifacts. Existing docs/device-validation.md records sourceSHA, runID, artifactSHA256, phone/OS, sideloader result and each USB experiment with time/revision/decoder observations; no invented passed rows.

## Dependencies and scope

Needs the preceding plan's implemented capability; creates the artifacts below. Sequential wave 2 follows shared-file ownership and the project's sequential execution setting.

<tasks>
<task type="auto" tdd="true">
  <name>01-02-T1: Download the exact successful native build safely from Windows</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>download_builds.py, download_builds.bat, tests/test_download_builds.py, build_config.json, .gitignore</files>
  <behavior>Explicit run or SHA never falls back silently; completed successful expected workflow/branch runs sort explicitly; checksum, manifest, wrong app structure, timeouts, gh errors and failed publication leave the previous IPA intact; artifact paths cannot escape staging.</behavior>
  <action>Implement the Windows launcher per D-02 using Python standard library and installed gh, deriving cwd from the launcher location. Read expected repo/workflow/branch/artifact names from build_config.json and accept --run-id and --sha. Run gh argument arrays with centralized timeouts; let gh own authentication, never extract tokens. Parse structured run JSON, require status completed and conclusion success, filter exact identity, sort by creation time and run ID, and print selected source SHA. Download only the expected artifact into a new temporary project-local directory. Check its manifest SHA/build identity and SHA-256; inspect ZIP members and require one immediate Payload/MusicPlayer.app with expected executable/plist. Reject path escapes/symlinks and unreasonable archive member totals before any extraction. Publish a new build directory atomically only after validation; retain existing GH_builds contents on any failure. Keep generated diagnostics English and redact credentials from subprocess errors. Provide concrete recovery without pretending auth failures are test success. Add the .gitignore entries per D-28, preserving /ref_pics/ and leaving local pictures untracked. Also retrieve the native evidence and matrix reports named in the successful artifact manifest; after verifying identity, copy native-evidence.json to build/native-evidence.json for the documented evidence-check command, keeping the original alongside its downloaded IPA.</action>
  <acceptance_criteria>From the repo root on Windows, unittest runs a nonzero test count and proves wrong-run/wrong-SHA/checksum/malformed archive/timeout cases fail with prior IPA preserved. Launcher passes user arguments and failure exit code.</acceptance_criteria>
  <verify>
    <automated>python -m unittest discover -s tests -p test_download_builds.py -v</automated>
    <fails_when>Nonzero exit, FAILED output, or Ran 0 tests in unittest output.</fails_when>
  </verify>
  <done>From the repo root on Windows, unittest runs a nonzero test count and proves wrong-run/wrong-SHA/checksum/malformed archive/timeout cases fail with prior IPA preserved. Launcher passes user arguments and failure exit code.</done>
</task>

<task type="checkpoint:human-action">
  <name>01-02-T2: Install the concrete artifact and observe Apple Devices transfers</name>
  <read_first>download_builds.bat and its tested behavior; actual successful native manifest; 01-RESEARCH.md USB readiness limitation; 01-CONTEXT.md D-12 through D-15.</read_first>
  <files>docs/device-validation.md</files>
  <action>Prepare the exact artifact and a short numbered procedure. Invoke the notification script only when the user's phone actions are needed. Have the user use their selected iLoader/SideStore setup, launch the app and record the actual installed SHA. Exercise individual WAV and MP3 transfer while app closed/open; bring it forward mid-copy; remove USB mid-copy including at a decodable MP3 frame boundary; retry overwrite; replace same filename; relaunch after an interrupted copy. Use the debug evidence already available to record resource identity, size, modification times, writer-visible final-name behavior and before/after parsing revisions. Record audible WAV playback after successful transfer and cable removal. Investigate whether the actual writer has reliable publication semantics; quiescence, stable attributes and full decodability alone do not prove the intended MP3 is complete. If no stronger completion evidence exists, show the observed limitation and concrete choice at this checkpoint: retain the selected route with explicit pending/unverified handling, or authorize a small source-size/hash manifest as an added transfer step. A mandatory manifest is a user workflow change and is never silently adopted. Do not mark LIB-01 fully met while that completeness question remains unresolved. Preserve actual relative paths for D-15 without claiming recursive directory transfer support; that belongs to Phase 2.</action>
  <acceptance_criteria>The user has launched the identified build and played an app-owned file after disconnect. Every named interruption/overwrite experiment has observations, and the publication rule to consume in Plan 03 is documented or explicitly unresolved with the user's choice pending.</acceptance_criteria>
  <verify>
    <automated>python scripts/native_check.py --check-evidence build/native-evidence.json --expected-sha HEAD</automated>
    <fails_when>Nonzero exit, source/toolchain/build mismatch, missing native evidence, or zero native tests. This command proves build identity only; it cannot pass the physical observations.</fails_when>
    <human-check>Installation and USB publication evidence must be supplied by the actual iPhone/Windows session. Record failures and unresolved MP3 intent-completeness limits.</human-check>
  </verify>
  <done>The user has launched the identified build and played an app-owned file after disconnect. Every named interruption/overwrite experiment has observations, and the publication rule to consume in Plan 03 is documented or explicitly unresolved with the user's choice pending.</done>
  <verification>Run the preceding automated evidence check and inspect the required human observations; missing or failed observations remain pending.</verification>
  <instructions>User installs the prepared IPA and performs the numbered Apple Devices experiments. The agent investigates results and implements fixes; only user pushes can trigger new source builds.</instructions>
  <resume-signal>Provide the recorded result and source SHA; a failed or missing result remains open.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust boundaries

GitHub artifact → Windows staging/publication; Windows USB writer → iOS shared Documents; unsigned IPA → user's sideloader.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-02-1 | Tampering | Artifact downloader | high | mitigate | Match repo/workflow/run/SHA, checksum and ZIP structure before atomic publication. |
| T-01-02-2 | Information disclosure | gh errors and diagnostics | high | mitigate | Keep credentials inside gh and redact subprocess output. |
| T-01-02-3 | Tampering | Interrupted/replaced transfers | high | mitigate | Collect actual publication/revision evidence; never treat quiet readable prefixes as completion. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Per D-01 and D-02, the Windows launcher retrieves the chosen successful build into GH_builds and the user installs that source SHA through iLoader/SideStore. Per D-03, the agent never pushes, and failed downloads preserve the previous usable IPA. Per D-12 through D-15, individual USB transfers are tested on the installed app; partial-copy publication facts are recorded before general readiness behavior is implemented.
</success_criteria>

## Flagged assumptions

EDGE BUILD-01/unclassified remains unresolved: installation/downloader assumptions are flagged until the actual run, download and phone evidence above. No inferred pass from the classifier. A1 transfer-completion behavior remains unresolved until this experiment; no arbitrary truncated-MP3 guarantee is asserted.

<output>
Create .planning/phases/01-installable-native-player/01-02-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
