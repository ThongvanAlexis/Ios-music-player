---
phase: 01-installable-native-player
plan: "18"
type: execute
wave: 18
depends_on: ["01-17"]
files_modified: ["MusicPlayer/Feature/DiagnosticView.swift","MusicPlayer/Diagnostic/DiagnosticFormatter.swift","MusicPlayer/Feature/SettingsView.swift","MusicPlayer/Feature/AppShell.swift","MusicPlayerUITests/DiagnosticViewTests.swift"]
autonomous: true
requirements: ["APP-02"]
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "Warnings remain reachable from every page until details are opened; successful playback never deletes retained diagnostic records or clears headphone-loss state."
    - "APP-02 empty edge: missing diagnostic fields use Not provided and zero records use exact approved empty copy."
    - "UI empty/diagnostics: No records use the Diagnostics empty copy from Copywriting Contract."
    - "UI loading/diagnostics: Available diagnostic records remain inspectable while history loads; any pending load is exposed as progress without disabling playback."
    - "UI error/diagnostics: Copy failure leaves full selectable text visible and uses the documented manual-copy recovery message."
    - "UI populated/diagnostics: Newest-first records expose operation, file, original message, domain, code, cause chain, recovery action and time."
    - "UI partial/diagnostics: Unavailable diagnostic fields display Not provided while available original causes remain intact."
    - "UI overflow/diagnostics: Record lists and full details scroll vertically; no original diagnostic message is truncated."
    - "UI zero-one-many/diagnostics: Zero uses the empty state; singular and plural failure counts agree with retained records."
    - "UI long-text/diagnostics: Long original messages and cause chains wrap fully and remain copyable without horizontal scrolling."
  artifacts:
    - path: "MusicPlayer/Feature/DiagnosticView.swift"
      provides: "Newest-first list, full details and Copy"
    - path: "MusicPlayer/Diagnostic/DiagnosticFormatter.swift"
      provides: "One full structured text representation"
    - path: "MusicPlayerUITests/DiagnosticViewTests.swift"
      provides: "Inspectable/copyable/warning behavior"
  key_links:
    - from: "MusicPlayer/Feature/AppShell.swift"
      to: "MusicPlayer/Feature/DiagnosticView.swift"
      via: "Persistent warning route on every destination"
    - from: "MusicPlayer/Feature/DiagnosticView.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticStore.swift"
      via: "Inspectable history independent of optional writer"
    - from: "MusicPlayer/Diagnostic/DiagnosticFormatter.swift"
      to: "MusicPlayer/Diagnostic/DiagnosticRecord.swift"
      via: "Complete original causes and recovery without replacement"
---

# 01-18: Inspect and copy retained errors from every page

**As a** native iPhone owner, **I want to** transfer local music from Windows over USB, install the native player through my existing sideload setup, and play it with app and system controls, **so that** my music stays available after disconnecting the PC and playback remains predictable when the phone locks or headphones disconnect.

<objective>
Show retained diagnostics with full selectable and copyable causes from every page.
Purpose: The technical user can understand and report failures while playback and navigation remain usable.
Output: MusicPlayer/Feature/DiagnosticView.swift, MusicPlayer/Diagnostic/DiagnosticFormatter.swift, MusicPlayer/Feature/SettingsView.swift, MusicPlayer/Feature/AppShell.swift.
</objective>

<execution_context>
@C:/Users/oliver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/oliver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-installable-native-player/01-CONTEXT.md
@.planning/phases/01-installable-native-player/01-RESEARCH.md
@.planning/phases/01-installable-native-player/01-PATTERNS.md
@.planning/phases/01-installable-native-player/01-UI-SPEC.md
@.planning/phases/01-installable-native-player/SKELETON.md

</context>

Use repository-root cwd. Follow the shared execution, documentation, naming and verification rules in SKELETON.md. Scripts and types named here are created by the indicated prior plan; no app source exists at planning time. Every native command prepares its pinned macOS toolchain and records current-source evidence. Windows source checks never substitute for Xcode or phone results. Only the user pushes.

## Artifacts this phase produces

NEW DiagnosticView shows a stable newest-first record list and detailed record sheet. NEW DiagnosticFormatter formats operation/file/original message/domain/code/cause chain/recovery/time with AppCopy labels and original Unicode values. Clipboard writing is an injected operation with actual UIPasteboard default and a testable failure result; it is not a forwarding class.

## Dependencies and scope

Needs Plan 17's implemented capability. This single-task expansion is separated for execution context size; its 5 modified files stay below the ten-file warning threshold without changing task behavior or acceptance. Sequential wave 18 follows the existing capability order and the project's sequential execution setting. The task files below define this plan's ownership; later capabilities remain assigned to their own expansion plans.

<tasks>
<task type="auto" tdd="true">
  <name>01-18-T1: Inspect and copy retained errors from every page</name>
  <read_first>AGENTS.md; 01-CONTEXT.md; 01-RESEARCH.md; 01-UI-SPEC.md; the existing files listed below before modifying them. Phase references resolve relative to this plan directory.</read_first>
  <files>MusicPlayer/Feature/DiagnosticView.swift, MusicPlayer/Diagnostic/DiagnosticFormatter.swift, MusicPlayer/Feature/SettingsView.swift, MusicPlayer/Feature/AppShell.swift, MusicPlayerUITests/DiagnosticViewTests.swift</files>
  <behavior>Zero/one/many records, missing fields and long causes remain fully visible; Copy receives the complete record; simulated copy failure leaves selectable text and approved recovery; opening detail acknowledges warning but never clears loss or pauses working audio.</behavior>
  <action>Implement the diagnostics UI per D-25/D-26 with exact AppCopy strings. Settings View Diagnostics and every persistent warning/Error Details route open the same retained history/detail screen. Use newest-first ordering with UUID/time tie-break, accurate singular/plural counts, a friendly summary then full original text/domain/code/file/causes/recovery/time, Not provided for absent fields, full wrapping vertical scroll and no technical text truncation. Copy Error Details is at least 44 pt and copies the complete formatter output. Use native pasteboard with injected write-result for tests; only show success once the operation confirms its write, otherwise retain selectable text and the specified recovery. Close Diagnostics is labeled and restores reading focus. Opening details acknowledges the visible warning while records remain; do not acknowledge headphone-loss manual-Play state. Already available history stays usable while persisted entries load, with nonblocking progress. Navigation and compact playback remain functional.</action>
  <acceptance_criteria>DiagnosticViewTests exercise every lifted diagnostics truth and prove full-copy equality, failure recovery, warning acknowledgment and unchanged playback/loss state.</acceptance_criteria>
  <verify>
    <automated>python3 scripts/native_check.py --ui-suite DiagnosticViewTests</automated>
    <fails_when>Nonzero exit, missing fresh result bundle/report for the current source SHA, any failed test, or zero executed tests for a requested test class.</fails_when>
  </verify>
  <done>DiagnosticViewTests exercise every lifted diagnostics truth and prove full-copy equality, failure recovery, warning acknowledgment and unchanged playback/loss state.</done>
</task>
</tasks>

<threat_model>
## Trust boundaries

Retained original diagnostics → native list/details and explicitly requested clipboard write.

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation plan |
|---|---|---|---|---|---|
| T-01-18-1 | Information disclosure | Diagnostic copy | medium | mitigate | Copy complete retained records only on explicit user action, retain selectable text on failure and avoid credential-bearing additions. |
| T-01-18-2 | Denial of service | Long original diagnostic text | medium | mitigate | Wrap and scroll complete causes while available history and shared controls remain usable during loading. |

Apply native-relevant ASVS level 1 controls; unresolved high or critical threats block completion. No app account or server exists in this phase.
</threat_model>

<verification>
Run each task's affected tests after its implementation and the integrated native suite after this wave. Record the exact source SHA, toolchain, destination and test counts. Human/device checks stay pending until actually exercised; later acceptance cannot infer a pass from source or screenshots.
</verification>

<success_criteria>
Warnings remain reachable from every page until details are opened; successful playback never deletes retained diagnostic records or clears headphone-loss state.
APP-02 empty edge: missing diagnostic fields use Not provided and zero records use exact approved empty copy.
UI empty/diagnostics: No records use the Diagnostics empty copy from Copywriting Contract.
UI loading/diagnostics: Available diagnostic records remain inspectable while history loads; any pending load is exposed as progress without disabling playback.
UI error/diagnostics: Copy failure leaves full selectable text visible and uses the documented manual-copy recovery message.
UI populated/diagnostics: Newest-first records expose operation, file, original message, domain, code, cause chain, recovery action and time.
UI partial/diagnostics: Unavailable diagnostic fields display Not provided while available original causes remain intact.
UI overflow/diagnostics: Record lists and full details scroll vertically; no original diagnostic message is truncated.
UI zero-one-many/diagnostics: Zero uses the empty state; singular and plural failure counts agree with retained records.
UI long-text/diagnostics: Long original messages and cause chains wrap fully and remain copyable without horizontal scrolling.
</success_criteria>

<output>
Create .planning/phases/01-installable-native-player/01-18-SUMMARY.md when execution is complete. Update validation evidence with actual results; planning alone does not mark any check passed.
</output>
